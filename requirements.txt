# 运行依赖（安装到 D 盘项目虚拟环境 .venv，见 docs/STATUS_AND_RISKS.md 安装约定）
langgraph==1.2.11
pytest==9.1.1
httpx==0.28.1
pydantic==2.13.5
# K4 PostgreSQL 唯一事实源（Repository 层；DB 需自行启动）
SQLAlchemy==2.0.52
alembic==1.19.1
psycopg2-binary==2.9.12
# 阶段三：LangGraph 可持久化 checkpoint（SQLite 文件；业务事实仍以领域服务/PG 为准）
langgraph-checkpoint-sqlite==3.1.1
# K5 FastAPI 接入层
fastapi==0.141.1
uvicorn[standard]==0.37.0
