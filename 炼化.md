# OpsPilot 项目熟悉与面试准备计划（炼化手册）

> 适用对象：项目作者本人，用于在面试前系统复盘本仓库（V1.1 历史快照；当前以 V1.2 整改勘误为准）。
> 依据：本仓库真实代码、测试与文档；所有基线数字以 `docs/TESTING_BASELINE.md` 与
> `README.md` 当前实测为准（本段保留的 **399 passed / 44 skipped** 与 **443 passed / 0 skipped**
> 是旧版本记录，不是当前验收数字；当前数字见下方 V1.2 整改勘误）。
> 用法：按阶段顺序学习；每阶段先读"重点阅读"，再自问"面试问题"，对照"解答要点"
> 检验理解深度；最后用文末"面试前自检清单"做整体自查。
> 纪律提醒：凡文档标注"未实测/未实现/实验"的能力（真实 LLM、微调、多实例部署、生产部署等），
> 面试表述必须与代码事实一致，不得把规划当已实现（AGENTS.md 宪法）。HTTP Agent 主链路已在 V1.2 接入。

---

## V1.2 整改勘误（2026-09-13）

本手册前文保留旧版本学习记录；面试和复盘时以本节及仓库当前代码、测试为准：

- 当前整改验收基线（0006 迁移后复验）：离线 `502 passed / 55 skipped`；隔离 PostgreSQL `557 passed / 0 skipped`；
  边界脚本 `scripts/review_v12_boundaries.py` memory 6 项 + PG 1 项全部 PASS（退出码 0）。
- HTTP Agent 生命周期已经接入：`start`、`clarify`、`decision`、`state` 均有受保护接口；只服务内部坐席，客户自助入口仍未开放。
- R1–R5 已完成整改：checkpoint v2 长度编码、HTTP 公共视图与错误响应脱敏、审计按线程实体隔离、保留原错误码的收尾恢复、独立进程重启恢复（按数据库时间判定租约过期）。
- 旧数字（399/44、443/0、490/…、494/543）只作历史留档，不得当作当前验收基线。
- PostgreSQL 是业务事实源，checkpoint 只保存流程恢复状态；所有演示数据为固定种子合成数据。
- 真实 LLM 指标、真实 Judge 分数、微调效果、性能吞吐、多实例部署和生产外部系统仍未实测或未实现，不应在面试中表述为已完成。

详细证据见 `docs/V1_2_REVIEW_2026-09-13.md`、`docs/STATUS_AND_RISKS.md` 和 `docs/TESTING_BASELINE.md`。

## 阶段总览（15 阶段，从整体到局部、从业务到技术）

| # | 阶段 | 对应维度 |
| --- | --- | --- |
| 1 | 项目定位、业务价值与诚实边界 | 项目背景与业务价值 |
| 2 | 总体架构、分层与信任边界 | 系统架构与模块划分 |
| 3 | 数据模型与 PostgreSQL 事实源 | 数据模型与数据库设计 |
| 4 | 确定性领域服务与"规则引擎"实现 | 规则引擎与安全考量 |
| 5 | 单 Agent 工作流：状态机、中断与恢复 | 状态管理（LangGraph checkpoint） |
| 6 | 幂等、并发控制与外部未知对账 | 可靠性、并发、幂等 |
| 7 | 本地 RAG：政策检索、评分与注入防护 | 检索与评分机制（对应"ICD 检索"的仓储替换项） |
| 8 | LLM 建议层：能力矩阵、内容守卫与降级 | LLM 建议层设计边界与降级策略 |
| 9 | 运行模式与四角色多 Agent 实验 | 多 Agent 实验与 ADR 取舍 |
| 10 | API 接入层、认证授权与本地工作台 | 安全合规、API 设计 |
| 11 | 评测体系：黄金集、回放、A/B 对照与影子评测 | 测试、评估与可观测性 |
| 12 | 测试策略、PG 隔离 guard 与 CI | 测试分层、CI、数据库迁移 |
| 13 | 安全与合规：审计、脱敏、提示注入、多租户 | 安全与合规 |
| 14 | 性能、扩展性与"不做"清单 | 性能优化与扩展性 |
| 15 | 难点复盘、失败案例与改进方向 | 难点、失败案例、改进思路 |

> 模板原始领域词（ICD 编码 / Celery / 医疗隐私）在本项目中的对应物已在相应阶段开头
> 注明映射关系，学习时按仓库真实技术回答，不要照搬模板术语。

---
# 阶段 1：项目定位、业务价值与诚实边界

**学习目标**：能一句话讲清"这是什么项目、解决什么问题、证明了什么"，并准确说出哪些
已实现、哪些只是实验/未实测——这是面试所有追问的底盘。

**重点阅读**：
- `README.md`（V1.1 主张、范围、当前验证、D 盘约束、面试版 90 秒介绍）
- `docs/INTERVIEW_OVERVIEW.md`（§0 一句话主张、§2 架构取舍、§3 安全不变量、§6 建议）
- `docs/STATUS_AND_RISKS.md`（状态矩阵与风险）
- `AGENTS.md`（宪法：职责分离、诚实、工具权限、评测纪律——全文应背下来）
- `docs/V1_EXECUTION_PLAN.md`（面试介绍话术模板 §"面试介绍"）

**核心概念与术语**：售后工单处置 Agent；可靠性（reliability）；确定性领域服务；
单 Agent 默认路径；四角色只读多 Agent 实验；PostgreSQL 唯一事实源；SQLite checkpoint
只存流程状态；黄金集；证据不足/越权/重复请求/工具失败/外部结果未知五类安全场景；
"证明 Agent 能在何时安全停止"的主张；V1 vs V1.1 vs 不纳入 V1.1 的边界。

**面试问题与解答要点**：

**Q1.1 请用 90 秒介绍你的项目：它解决什么问题，为什么有价值？**
解答要点：
- 定位：企业售后工单处置的 **Agent 可靠性演示项目**（非生产系统）：证明 Agent 能在
  受控权限内完成"退款工单"多步骤流程，并在**证据不足、越权、重复请求、工具失败、
  外部结果未知**时安全停止、等待或转人工。
- 为什么有价值：LLM/Agent 应用最大的工程风险不是"答得漂不漂亮"，而是"幻觉金额、
  越权动作、重复副作用、状态错乱"。本项目把"语言能力"与"业务裁决"**彻底分离**：
  Agent 只做意图、澄清、检索与解释；金额/资格/权限/状态/审批/幂等/审计全部由
  **确定性领域服务 + PostgreSQL** 裁决。价值主张 = "让不可靠的模型永远碰不到
  不可逆的副作用"。
- 证据：不是 PPT——399/443 测试、七场景演示脚本、11/11 与 120/120 黄金集回放、
  PG 隔离双跑、GitHub Actions CI 全绿（run #1 success）。面试必须能报出这些真实数字
  和跑法。
- 不说谎：真实 LLM 未接入（离线规则实测）、未微调、非生产部署——主动说出边界反而加分。

**Q1.2 为什么"单 Agent"而不是"多 Agent"作为默认？**
解答要点：
- 事实：用 `evals/compare_agents.py` / `evals/compare_modes.py` 在**同一 golden_v1**
  上对照，单 Agent 与 Supervisor（三 agent/四角色）的通过率、outcome、退款金额全部
  11/11 一致——确定性规则实现下**没有可量化的业务收益**。
- 取舍：多 Agent 只增加协调复杂度与故障面（角色间消息、trace、超时、权限面），而本项目
  要证明的核心可靠性问题（确定性裁决）**不依赖多角色**就能解决。
- 依据：ADR-002 回退条款——"拆分无收益则回退到 supervisor 下的单 Agent 路径"。
- 保留多 Agent 的原因：作为**可选实验运行时**（`SupervisorRunner(orchestration="four-role")`），
  展示角色化轨迹，也是未来真实 LLM 子 Agent 的挂载点。
- 面试话术：不要吹"我们用了多 Agent 架构"，而要说"我对照过、没收益、所以默认单 Agent，
  多 Agent 保留为可复现实验"——这体现用数据做架构决策。

**Q1.3 项目里"可靠性"具体指什么？有哪些量化证据？**
解答要点：
- 指"安全不变量"零违例：越权成功 0、重复副作用 0、未知状态换键重试 0、非法状态迁移 0、
  审批伪造 0、提示注入放行 0（`docs/INTERVIEW_OVERVIEW.md` §3 表格）。
- 量化证据：内存全量 399 passed/44 skipped；隔离 PG 443 passed/0 skipped；演示七场景
  全过；golden_v1 11/11、golden_v2 120/120；缺陷回归台账 D1–D12 各自有回归测试。
- 说明哪些是"演示/合成"：全部数据为固定种子合成数据，不代表真实企业收益（宪法第二条）。
- 如果被追问"真实模型下这些不变量还成立吗"：诚实回答——真实 LLM 未实测；设计上靠
  **能力矩阵默认禁用高风险能力 + 内容守卫 + 领域服务终裁**保证即使模型输出异常也无法
  越权，这是未来接入真实 LLM 时的不变量设计，而非已证结论。

**Q1.4 V1、V1.1 与"不纳入"边界分别是什么？为什么主动不做某些事？**
解答要点：
- V1：单 Agent 售后闭环 + 确定性领域服务 + PG 命令路径 + 黄金集/回放/演示。
- V1.1：四角色只读多 Agent（实验）、确定性证据检索基线（本地 RAG）、运行模式开关、
  受控 LLM 适配层（离线基线实测）、微调样本生成入口（骨架）、Agent Lab 工作台。
- 不纳入 V1.1：真实 LLM 指标、真实微调、真实 Mule/MCP、pgvector、长期记忆、前端工作台
  生产化、生产部署、真实支付/CRM。
- 为什么不做：项目是"可靠性证明"作品，不是"功能堆叠"。每一项扩展都需要**对照组证据**
  （宪法第七条/第十条），没有证据的"技术名词"只会稀释可信度。
- 面试升级表达：主动说出"我知道 pgvector/长期记忆/微调是热门，但我在没有可量化收益与
  安全设计前不引入"，比被动被问更显工程判断力。

**Q1.5 如果让你把这个项目再往下做一个季度，你会优先做什么？**
解答要点（与 `docs/INTERVIEW_OVERVIEW.md` §6 一致）：
- 优先 ①：把 FastAPI 与 WorkflowRunner 合并为一条 **HTTP Agent 主链路**（补
  start/clarify-resume/state 接口 + 浏览器 e2e）——当前 API 与 Agent 运行器是两个入口，
  这是叙述上最大的诚实短板，也是价值最大的增量。
- 优先 ②：真实 LLM 影子评测（需安全 Key + 白名单 Base URL），建立失败样本集——为微调
  与否提供对照数据。
- 优先 ③：审计检索/限流/CORS/OpenAPI 运营观测等低风险增量。
- 原则：任何新增都先写 RED 测试与真实评测，不引入未经证实的名词。

---
# 阶段 2：总体架构、分层与信任边界

**学习目标**：能徒手画出架构图（FastAPI / WorkflowRunner / 领域层 / PG / SQLite 的位置），
说清每层"负责什么、不负责什么"，并解释两个入口与 trust boundary。

**重点阅读**：
- `docs/ARCHITECTURE.md`（全文；组件职责表最重要）
- `docs/INTERVIEW_OVERVIEW.md` §1 时序图、§5 主架构图
- `src/domain/after_sales/ports.py`（AfterSalesApplicationPort Protocol）
- `src/agents/ports.py`（AfterSalesGateway）
- `src/domain/after_sales/adapters.py`（MemoryAdapter / PgCommandAdapter）
- `src/api/runtime.py`（启动装配纪律）、`src/agents/runner.py`（WorkflowRunner 头部）
- `src/__init__.py`、`src/platform/__init__.py`（总览注释）

**核心概念与术语**：分层职责；依赖倒置（Port/Adapter）；MemoryAdapter vs PgCommandAdapter；
双入口（API 入口 = 领域服务接口；Agent 入口 = WorkflowRunner 脚本/回放）；checkpoint 不存
业务真相；tenant-first；确定性证据检索基线（本地 RAG）只作证据；"模型不可信、领域可信"。

**面试问题与解答要点**：

**Q2.1 画出你的系统架构，并说明每一层为什么这样切。**
解答要点：
- 四层：① 接入层 FastAPI（路由只适配/编排，不复制领域逻辑）；② Agent 层 LangGraph
  （WorkflowRunner / SupervisorRunner，负责语言任务与编排）；③ 领域层
  `src/domain/after_sales`（确定性规则 + 服务，唯一业务裁决者）；④ 存储层 PG（业务事实源）
  + SQLite（仅 LangGraph checkpoint）。
- 为什么这样切：**按"可信度"切分而不是按功能切分**——模型层与工具可能不可靠，所以把
  一切会改变业务事实的决定下沉到纯函数 rules + 单事务命令服务；上层只做只读编排与展示。
- 依赖方向：API/Gateway/Runner 都只依赖 `AfterSalesApplicationPort` 抽象（MemoryAdapter/
  PgCommandAdapter 均可，不 isinstance 分支）——这就是为什么能在内存跑测试、在 PG 跑
  生产语义而不改上层。
- 画图注意：把 FastAPI 与 WorkflowRunner 画成**并列的两个入口**（诚实），PG 下方标注
  "唯一业务事实源"，SQLite 标注"仅流程状态"。

**Q2.2 为什么需要 Port/Adapter 抽象？直接用 AfterSalesService 不行吗？**
解答要点：
- 为什么：① 内存实现（AfterSalesService + dict 存储）用于测试/演示/单测，快且无需外部
  依赖；PG 实现（PgCommandService + Repository）是生产语义。上层（Agent、API、评测回放）
  要能在两者上跑同一套逻辑（黄金集 A/B 就依赖这点）。② 抽象使"确定性领域规则"与
  "持久化实现"解耦，防止 PG 细节泄漏到 Agent 编排。③ 便于未来换存储/加 profile。
- 如果不这样做：上层会写 `isinstance(backend, PgCommandAdapter)` 之类的分支，或直接依赖
  SQLAlchemy session，测试必须起真 PG，黄金集对比也无法在内存快速跑。
- 具体实现：`AfterSalesApplicationPort` 是 Protocol（结构类型），定义了 create_ticket/
  create_refund_draft/submit/approve/reject/execute/reconcile/close_ticket/get_* 等；
  `MemoryAdapter` 包 `AfterSalesService`，`PgCommandAdapter` 包 `PgCommandService`
  （后者内部是"单事务读最新事实 → rules 校验 → 写业务行+幂等+审计 → 提交"）。
- 局限（主动说）：PG 命令路径当前未实现全部 Port 方法时上层仍需小心（契约测试
  `test_repo_contract`/phase4 RED 测试兜底）；且 PG profile 启动要求高（见阶段 10）。

**Q2.3 "API 与 Agent 运行器是两个入口"是什么意思？为什么这是必须诚实说明的点？**
解答要点：
- 现状：FastAPI 验证的是**领域服务接口**（create ticket/refund draft/approve/execute…），
  角色门禁与审批边界都在 API 层测过；但 **Agent 的启动、澄清、恢复仍由脚本/回放驱动**
  （WorkflowRunner，demo/evals 调用），`create_app` 工厂没有把 LangGraph 图接进 HTTP。
- 为什么不合并：合并 = 把 start / clarify-resume / state 变成 HTTP 接口 + 浏览器 e2e，
  工作量真实存在；当前项目以"确定性领域 API + Agent 回放/演示"为默认验收路径。
- 不这样说的后果：面试若宣称"我的 API 支持 Agent 澄清恢复"，会被代码审查打脸——
  `docs/ARCHITECTURE.md` 明确写"不能宣称 HTTP 已覆盖 Agent 启动、澄清和恢复"。
- 改进方向：优先级最高的一项增量（补 HTTP Agent 主链路 + e2e）。

**Q2.4 解释时序图里"审批 interrupt → resume 重读领域决定"为什么重要。**
解答要点：
- 流程：草稿落库 → interrupt(approval) → 授权人在领域服务提交**带版本** approve/reject →
  resume 触发 `apply_decision` **重读领域事实**（不是读 resume 参数里的"通过"字样）。
- 为什么：审批决定是"已提交且带版本的领域事实"（approval_decisions 表 + UNIQUE
  (tenant,operation,decided_version)）；若 resume 参数自带"我批准了"语义，伪造请求就能
  放行（有 g11 伪造 resume 回归测试）。
- 不这样做会怎样：checkpoint/resume 变成可注入的"审批通道"，安全不变量"审批伪造 0"
  立即被击穿。
- 面试加分点：强调"resume 只触发重读，业务语义永远来自数据库事实 + 版本 CAS"。

**Q2.5 这张架构图里最薄弱的环节是什么？你会怎么补强？**
解答要点（开放题，展示自我批判）：
- 候选答案：① 双入口未合并（HTTP 缺 Agent 生命周期）；② 真实 LLM 未实测，离线规则的
  不变量结论不能外推到真模型；③ PG profile 需本地 Docker + alembic，演示环境依赖；
  ④ 本地工作台（src/api/ui + Agent Lab）只读隔离，不是生产后台。
- 补强思路：先补 HTTP e2e 主链路（按 INTERVIEW_OVERVIEW §6 排序），再引真实 LLM 影子
  评测；每个补强都先写 RED 测试。
- 表达方式：不要等面试官找茬，主动暴露并给排序方案，展示工程判断与诚实。

---
# 阶段 3：数据模型与 PostgreSQL 事实源

**学习目标**：能讲清 10 张业务表的职责、为什么 PG 是唯一事实源、schema 如何支撑
"版本 CAS / 幂等三元组 / 租约 / 审计不可变"等语义；理解 memory 与 PG 的关系。

**重点阅读**：
- `src/repo/schema.sql`（全文；含每段注释——作者自己写的设计说明）
- `alembic/versions/0001..0005_*.py`（迁移演进顺序：init → CHECK → ticket meta →
  policies/order_items/entity_seq → approval 唯一性/幂等三元组/workflow_threads）
- `docs/POSTGRES.md`（本地开发、验收隔离、guard 语义）
- `src/repo/interfaces.py`（Row dataclass 与 Repository 契约、UniqueViolation/OptimisticLockError）
- `src/repo/postgres.py`、`src/persistence/row_codecs.py`（行 ↔ 领域对象）
- `src/domain/after_sales/models.py`（金额 Decimal、状态机枚举）
- `docs/INTERVIEW_OVERVIEW.md` §3（数据支撑的不变量）

**核心概念与术语**：多租户 `(tenant_id, xxx)` 复合主键；numeric(12,2)；版本列（乐观并发）；
幂等三元组 (tenant_id, command_type, raw_key) 唯一约束；approval UNIQUE
(tenant,operation,decided_version)；追加式审计（audit_events 无 UPDATE/DELETE 语义）；
workflow_threads 租约表（lease_owner/lease_until/generation/recovery_meta，不含业务真相）；
entity_seq 租户内自增；alembic_version；CHECK 约束（状态枚举/金额非负/比例 0-1/生效期）；
SQLite 仅 LangGraph checkpoint（WAL + busy_timeout）；memory 后端是"测试/演示"不是事实源。

**面试问题与解答要点**：

**Q3.1 为什么说 PostgreSQL 是"唯一业务事实源"？SQLite 和内存存储各扮演什么角色？**
解答要点：
- PG 保存订单、工单、退款操作、审批决定、幂等记录、审计、政策、工作流租约——一切
  "业务真相"。恢复/重放/并发安全都依赖它。
- SQLite 只被 `SqliteSaver` 用作 **LangGraph checkpoint**：保存节点位置、请求指纹、
  租户、最小流程字段，用于进程重启后 resume；**禁止**在里面存金额/审批/执行结果
  （checkpoint 可丢失、损坏、被伪造——有注入防护与 fail-fast 测试）。
- 内存（AfterSalesService + dict）是**测试/演示/Agent Lab 沙箱**，生产不得使用；
  因为数据在进程内、无跨进程一致性与持久性。
- 为什么不把所有东西都塞 SQLite checkpoint：那会让"流程状态"变成第二事实源，恢复时
  从 checkpoint 覆盖业务真相，违背"恢复一律重读领域服务"的不变量（D7/K3 相关）。
- 面试反问自检：能解释"checkpoint 里即使有 amount 字段也只是展示快照，不是裁决依据"吗？

**Q3.2 解释幂等三元组与 approval 唯一约束的设计；如果漏掉会出什么问题？**
解答要点：
- 幂等：`idempotency_records` 唯一 `(tenant_id, command_type, raw_key)`，配合
  `payload_hash`（同键同载荷返回原结果；同键异载荷拒绝）。`command_type` 进键使"退款"
  与"建单"可用同一 raw_key 而互不冲突；`tenant_id` 进键保证多租户幂等隔离（D2）。
  前缀规范键 `f"{tenant}:{cmd}:{raw_key}"`（0005 前的 D2 语义）保留为冗余展示列。
- 审批：approval_decisions UNIQUE `(tenant_id, operation_id, decided_version)`——同一版本
  只允许一个决定；配合 `update ... WHERE version=expected`（乐观锁），并发 approve/
  reject 只有一个成功（D4-D5）。
- 如果漏掉：重复点击/重试/网络重发会造成**重复退款副作用**；两个审批人同时批同一版本
  会产生矛盾决定。都是阻断级安全不变量（重复副作用 0 / 审批伪造 0）。
- 实现位置：约束在 schema（数据库兜底），语义在领域 rules + PgCommandService 单事务。

**Q3.3 多租户在数据模型层面如何落地？跨租户访问为什么会被挡住？**
解答要点：
- 每张业务表都有 tenant_id，主键是 `(tenant_id, 业务id)`——**同一 order_id 在不同租户
  可以共存**（D1：内存 seed 也拒绝跨租户覆盖，因为 dict 以 order_id 为键；PG 语义正确）。
- Repository 接口全部以 tenant_id 显式作用域（get_operation(tenant_id, operation_id)…），
  API 层 tenant 来自认证身份（请求体不带 tenant_id，不信任请求体）。
- 效果：跨租户读 → 内存 403 TENANT_MISMATCH / PG 404 NOT_FOUND（不泄露存在性）；
  跨租户写被租约/门禁拒绝；RAG citation 校验也要求同租户。
- 面试深层：为什么 PG 用 404 而 memory 用 403？——PG 查不到即"不存在"，避免向无权限
  者暴露资源存在性；两种都要回归测试（D1/D3）。

**Q3.4 workflow_threads 表是干什么的？为什么它"不能替代业务事实"？**
解答要点：
- 表结构：tenant_id+thread_id 主键、status、request_fingerprint、lease_owner/lease_until、
  generation、recovery_meta（JSON：节点/最小恢复元数据）。
- 作用：跨进程并发 resume 同一线程时，只有**持约者**能读 checkpoint/推进图（原子
  claim，owner_id 稳定唯一 + 过期接管）；失约者抛 ThreadLeaseError **零副作用**（D9）。
  request_fingerprint 保证"同一 thread 只能继续原请求"，换请求复用线程被拒。
- 为什么不能替代业务事实：它只存"谁可以推进流程"，不含订单/金额/审批决定；业务真相
  仍在业务表，resume 后从领域服务重读。
- 不这样做会怎样：两个进程同时 resume → 双重执行退款（重复副作用）；或 A 线程的请求
  混入 B 线程上下文（串话）。PG 事务 + 行锁/租约是跨进程唯一可靠手段，内存锁不够。

**Q3.5 alembic 迁移 0001→0005 的演进逻辑是什么？从中能看出什么设计过程？**
解答要点：
- 0001 初始售后 schema（orders/tickets/refund_operations/approval_decisions/
  idempotency_records/audit_events）→ 0002 数据库级 CHECK（状态枚举、金额、比例）→
  0003 工单扩展列（created_by/reason_tags）→ 0004 PG-first 命令数据模型
  （policies 版本化政策、order_items、entity_seq 租户内自增）→ 0005 phase4：
  approval 唯一性、幂等三元组、workflow_threads 租约。
- 设计信号：约束从"应用层"逐步下沉到"数据库层"（CHECK + UNIQUE + FK + 行锁），
  反映作者意识到"只靠应用代码校验不够，数据库是最后防线"；版本 CAS 与幂等唯一
  都在 0005 收口，说明这是并发安全主题的集中补强。
- 面试可用：主动讲"我把状态枚举、金额、比例都做成 DB CHECK，因为规则纯函数可能在
  某条路径漏调用，DB 约束是 fail-closed 的最后一道闸"。

**Q3.6 金额为什么用 Decimal + numeric(12,2) 而不是 float？在哪些环节会遇到坑？**
解答要点：
- Python float 二进制无法精确表示 0.1，累计退款比较/求和会产生 0.30000000000000004 类
  误差；退款金额差一分就是资损/审计问题。
- 落地：领域对象统一 Decimal（`parse_money` 解析），DB 用 NUMERIC(12,2)；schema 还有
  `CHECK (amount IS NULL OR amount > 0)`、`paid_amount >= 0`、`refund_ratio BETWEEN 0 AND 1`。
- 坑位：任何从 JSON/字符串回传金额的地方要显式 Decimal(str(...))，禁止 float() 中转；
  JSON 序列化时转字符串（如 `str(p.amount)`）保持精度；DB 求和返回 Decimal 再比较。
- 如果面试官问"为什么不用 int 分单位"：诚实说那是另一种有效方案（整数分避免精度问题），
  本项目选 numeric(12,2) 便于审计可读性与 SQL 聚合，两种都行但必须全链路一致。

---
# 阶段 4：确定性领域服务与"规则引擎"实现

> 模板领域词映射：本项目的"规则引擎/ICD 检索评分"对应两个真实模块——(a) 确定性政策
> 匹配与冲突检测（`policies.py` + `rules.py`），(b) 本地 RAG 检索评分（阶段 7）。
> 本阶段覆盖 (a) 与命令事务；(b) 在阶段 7 单独展开。

**学习目标**：吃透领域层"权限→政策→金额→状态机→幂等→审计"的裁决链；能解释
Memory 路径与 PG 命令路径如何共用同一套纯函数 rules；理解"单事务八命令"。

**重点阅读**：
- `src/domain/after_sales/service.py`（AfterSalesService 完整命令路径与导出/恢复）
- `src/domain/after_sales/rules.py`（纯函数：transition/role/amount/capacity/version）
- `src/domain/after_sales/policies.py`（PolicyRule、match_policies、detect_conflict）
- `src/domain/after_sales/models.py`（错误码 AfterSalesErrorCode、状态机、状态迁移表）
- `src/domain/after_sales/pg_commands.py`（PgCommandService 单事务八命令）
- `src/domain/after_sales/ports.py`、`adapters.py`
- `tests/unit/domain/after_sales/test_rules.py`、`test_pg_commands.py`、
  `tests/integration/test_pg_commands_live.py`

**核心概念与术语**：规则纯函数单一事实源；权限模型（agent/customer/approver/system）；
政策版本/生效期/窗口/诉求标签；冲突政策转人工（POLICY_CONFLICT）；无政策转人工
（POLICY_NOT_FOUND）；退款上限（累计已执行 ≤ 实付）；状态机（ticket + operation）；
草稿→审批→执行/unknown 对账；版本 CAS；订单行锁容量保护；稳定错误码；单事务多写。
"金额唯一来源 = compute_refund_plan / 领域计划"。

**面试问题与解答要点**：

**Q4.1 领域层如何保证"金额、资格、权限"不被 Agent 或 LLM 决定？**
解答要点：
- 结构保证：写路径只经 AfterSalesApplicationPort 的命令（create_refund_draft 等），
  Agent/Gateway/API 无任何绕过通道；approve/reject 需要 APPROVER 角色 + decided_by 认证
  身份；execute 仅 SYSTEM；reconcile 仅 SYSTEM。
- 语义保证：`compute_refund_plan` 是金额唯一来源——内部走 `match_policies` + 政策
  refund_ratio × paid_amount（累计上限内），Agent 的 action_draft 只有展示用的 amount
  字符串（来自领域返回值），四角色 Resolution schema **没有金额字段**；模型层内容守卫
  拦截金额词。
- 为什么有效：不是"约定 Agent 别乱来"，而是"Agent 根本没有写金额的通道 + 模型输出
  即便写了金额也被守卫拒绝 + 领域重算"三道闸。
- 若被追问"如果 agent 伪造一个 create_refund_draft 调 amount=99999？"：命令服务内部会
  用**政策规则重算**并校验容量/权限/租户，入参 amount 只是草稿输入而非放行值（PG 命令
  路径以领域计算为准），越权 amount 超上限 → AFTER_SALES_AMOUNT_EXCEEDS_REMAINING。

**Q4.2 政策匹配与冲突检测是怎么实现的？为什么"冲突"要转人工而不是取平均/取最大？**
解答要点：
- PolicyRule：tenant/request_type/reason_tags/window_days/refund_ratio/effective_from/
  version。`match_policies` 按 tenant+request_type 找生效期内政策，诉求标签须**覆盖**
  政策标签；命中多条且 refund_ratio 不一致 → `detect_conflict` 报 POLICY_CONFLICT。
- 为什么转人工：政策冲突说明业务口径本身有歧义或版本更替未收敛，此时"取平均/取最大"
  是替业务拍板——退款比例是资损敏感决策，宁可转人工不可静默猜（宪法第六条：无可靠证据
  必须澄清/拒答/转人工）。同一政策多版本取 **effective_from ≤ today 的最新 version**
  （未来版本不参与当前决定）。
- 不这样做会怎样：规则层静默选一条 → 可能给客户错误比例或资损，且不可审计为什么选了
  那条。转人工则把歧义显式交给授权人员。
- PG 实现：`_effective_policy_rows` 先在 SQL/行层过滤 tenant/request_type/生效期/标签
  覆盖/版本最大，再交 match_policies 纯函数，保持"规则唯一事实源"。

**Q4.3 解释 ticket 状态机与 operation 状态机；为什么状态迁移也做成纯函数并加 DB CHECK？**
解答要点：
- ticket：open→resolved/rejected/closed 等（TICKET_TRANSITIONS）；operation：draft→
  pending_approval→approved/rejected→executed，以及 unknown/failed、终态集合
  TERMINAL_OPERATION_STATUSES。`check_ticket_transition/check_operation_transition` 是
  纯函数（当前状态→目标状态是否合法），内存与 PG 命令路径共用。
- 为什么纯函数 + 双保险：单一规则事实源避免两套实现漂移（service.py 用 rules.py，
  pg_commands.py 也用 rules.py）；DB CHECK 兜底任何绕过应用层的非法写入。
- 版本 CAS：`update ... SET ... WHERE version=expected`，冲突抛 DECISION_VERSION_MISMATCH
  （旧决定被拒）；并发 approve+execute 恰一成功（D4-D6 回归）。
- 不这样做会怎样：状态可回跳/跳级（如未审批直接 executed），审计与合规失效；并发下
  旧版本决定覆盖新决定。

**Q4.4 PgCommandService "单事务八命令" 的完整路径是什么？崩溃/异常时会发生什么？**
解答要点：
- 八命令：create_ticket / create_refund_draft / submit / approve / reject / execute /
  reconcile / close_ticket。
- 每命令 = 一个数据库事务：BEGIN → 事务内读最新 Row → rules 纯函数校验（权限/租户/
  版本 CAS/状态机/金额/容量）→ 写业务行 + 幂等记录（唯一约束冲突→回滚） + 追加审计 →
  COMMIT → 返回领域对象。
- 异常/冲突 → 整体 ROLLBACK（无部分提交：不会出现"业务行写了但审计没写"的脏状态，D7）。
- 容量保护：execute/reconcile 前对 orders 行 `SELECT ... FOR UPDATE`（订单级锁），
  累计 executed 退款 SUM + 本次 ≤ paid_amount，否则拒绝。
- 面试细节：能说出"读-校验-写在同一事务、锁在事务内获取"才是并发安全的，否则 check
  then act 存在窗口。

**Q4.5 规则错误码为什么用稳定枚举（AFTER_SALES_*）而不是任意字符串？**
解答要点：
- AfterSalesErrorCode 枚举（OPERATION_NOT_FOUND/POLICY_NOT_FOUND/POLICY_CONFLICT/
  TENANT_MISMATCH/DECISION_VERSION_MISMATCH/AMOUNT_EXCEEDS_REMAINING/PERMISSION_DENIED…）
  全链路透传：领域抛 → Adapter/Gateway 原样透出 → Agent state.error_code →
  API 错误处理映射 HTTP 状态。**错误码不被模型/Agent 改写**（规则引擎返回的错误不得被
  模型改写成成功，宪法第六条）。
- 为什么：稳定错误码让上层（Agent 决策转人工、API 响应、评测断言、审计）可编程依赖，
  不会因 LLM 措辞变化而漂移；也是黄金集"安全不变量"断言的基石。
- 若用自由文本：上层无法可靠判断"没找到订单"vs"网络错误"，Agent 可能把失败当成功
  继续编草稿。

**Q4.6 Memory 服务与 PG 命令服务如何保证语义一致？有没有刻意保留的差异？**
解答要点：
- 语义一致：两者共用 `rules.py` 纯函数与同一 domain models（状态机、错误码、
  金额类型），上层都走 AfterSalesApplicationPort；契约/回归测试同时跑两端。
- 刻意差异：持久化与并发机制不同——Memory 用进程内 dict + per-key/order 锁 +
  get_or_reserve 占位；PG 用表约束 + 行锁 + 乐观版本 + 事务回滚。因此：
  (a) PG 下多进程并发才真正安全；(b) memory 的 seed_order 拒绝跨租户同 order_id 覆盖
  （dict 键限制），PG 用 (tenant,order_id) 主键天然支持多租户同 id。
- 诚实边界：memory 是测试/演示/沙箱语义，面试别把 memory 的并发结论说成生产结论；
  生产语义以 PG live 测试为准。

---
# 阶段 5：单 Agent 工作流：状态机、中断与恢复

> 模板领域词映射：模板的"异步任务与状态管理（Celery）"在本项目对应 **LangGraph 状态机 +
> 显式 interrupt/resume + SQLite checkpoint**（不是 Celery 队列）。面试时若被问"你用过
> Celery 吗"，如实说本项目刻意用 LangGraph 的图内 interrupt 表达人工审批/澄清等待，
> 并说明两类方案分别适合什么（见 Q5.6）。

**学习目标**：能讲清 LangGraph 图节点、条件路由、两处 interrupt、resume 重读语义、
线程生命周期（指纹/租约）、以及"checkpoint 不存业务真相"如何在代码里落实。

**重点阅读**：
- `src/agents/state.py`（AgentState TypedDict，必含字段）
- `src/agents/graph.py`（build_workflow：节点与条件边全图）
- `src/agents/nodes.py`（parse/clarify/gather_evidence/plan/create_ticket_draft/
  request_approval/apply_decision/execute_operation/settle_rejected/escalate）
- `src/agents/runner.py`（WorkflowRunner：start/submit_decision/resume；指纹与租约）
- `src/agents/checkpoint.py`（SqliteSaver，WAL/busy_timeout/fail-fast）
- `src/agents/intent.py`（确定性意图与字段抽取）
- `tests/unit/agents/test_workflow_happy_path.py`、`test_clarify.py`、
  `test_approval_rejected_and_forgery.py`、`test_resume_idempotency_and_unknown.py`、
  `test_thread_lifecycle.py`、`test_sqlite_persistent_checkpoint.py`
- `scripts/demo_interview.py`（七场景：看代码如何逐步断言）

**核心概念与术语**：AgentState（TypedDict，必含字段：tenant/thread/ticket/intent/
missing_fields/evidence_refs/action_draft/risk_level/approval_id/operation_id/
next_action/error_code/audit_event_ids）；StateGraph；条件边；interrupt 值
（type=clarify/approval）；resume 重读；请求指纹（sha256(tenant|order|request)）不可变；
ThreadConflictError/ThreadLeaseError；waiting_approval/waiting_clarify；outcome 枚举
（refunded/rejected/escalated/operation_unknown/already_executed/cancelled/no_action）；
"Agent 不猜金额/不伪造审批/不换键重试"。

**面试问题与解答要点**：

**Q5.1 描述单 Agent 的完整节点流：从一句话到退款落库，中间哪里会停下等人？**
解答要点：
- parse（意图+缺参）→ 缺参则 clarify interrupt（补参后回 parse 再判定）→
  gather_evidence（只读订单/历史/物流/政策检索）→ plan（领域 compute_refund_plan，
  金额唯一来源）→ create_ticket_draft（写草稿+工单）→ request_approval interrupt
  （等人批）→ resume 后 apply_decision（重读领域决定，非 resume 参数）→
  依据决定走 execute_operation（SYSTEM 模拟外部执行）或 settle_rejected 或 escalate；
  执行外部超时 → operation_unknown → reconcile_required。
- 两处 interrupt：clarify（信息不足，不猜）与 approval（高风险动作，等授权）。
- 每步写回 AgentState 的字段可复述（比如 error_code 填领域错误码；audit_event_ids
  记录触发过的审计事件），体现你真的读过 state schema。
- 面试画图：把图从 START 到 END 走一遍，特别标出两条 interrupt 边是"图内 interrupt"
  而不是"分支结束"。

**Q5.2 为什么澄清/审批要用"中断-恢复"而不是一轮对话直接做完？**
解答要点：
- 澄清：缺订单号/诉求时不能猜政策与金额——必须 interrupt 收集信息后回到 parse 重新
  判定（可能多轮澄清）。LangGraph interrupt 天然把"等待外部输入"建模为图暂停点，
  checkpoint 保存位置。
- 审批：退款是高风险副作用，授权人可能离线/需要时间；流程必须挂起而不是超时失败。
- 与异步队列的区别：审批是人参与、时长不确定（分钟~天）的事件，队列 worker 的
  ack/重试模型不适合"无限期等待 + 幂等恢复"；图 checkpoint + 领域决定重读更适合。
- 恢复语义：resume 不重放副作用（checkpoint 从原节点继续），业务真相重读领域服务，
  同幂等键重复 resume 返回原结果。

**Q5.3 "resume 只触发重读，不携带业务决定"——这句话在代码里怎么体现？有测试吗？**
解答要点：
- 代码：runner.start/submit_decision/resume 中，审批决定只经 `submit_decision` 提交到
  领域服务（approve/reject 命令），resume 参数只是"继续执行"信号；apply_decision 节点
  从领域服务查询 operation/approval 事实（含 decided_by/version）决定去向。
- 伪造防护：AgentState 里即便有人塞一个 decision="approved" 也不被采信（决策来自领域
  重读）；g11 伪造 resume / `test_approval_rejected_and_forgery.py` 断言伪造恢复被忽略。
- 为什么重要：审批通道若能被 resume 参数伪造，安全不变量"审批伪造 0"立即失效，
  授权模型（只有 approver 角色能提交决定）也被绕过。
- 面试反例准备：被问"那我把 resume 参数里加 approved=true 会怎样？"——答：会被忽略，
  行为由 approval_decisions 里带版本与 decided_by 的真实记录决定。

**Q5.4 同一线程的生命周期如何防"串请求"与"双推进"？（指纹 + 租约）**
解答要点：
- 指纹：start 时用 tenant + 规范化请求 + order 提示生成 `request_fingerprint`（sha256）
  写入 checkpoint，**不可变**；同一 thread_id 提交**不同请求** → ThreadConflictError
  （拒绝复用线程），同请求重复提交返回原结果。
- 租约（PG profile，D9）：workflow_threads 表原子 claim（lease_owner 稳定唯一 +
  lease_until 过期 + fingerprint 一致）；持约者才能读 checkpoint/推进图；失约 →
  ThreadLeaseError 零副作用；进程崩溃后其他同 fingerprint owner 可过期接管。
- 为什么两层都要：指纹防"逻辑上串话"（换请求），租约防"物理上双推进"（跨进程并发），
  单靠内存锁无法覆盖多进程。
- 对应测试：test_thread_lifecycle / test_thread_tenant_namespace /
  test_thread_fingerprint_tamper / phase4 test_runner_lease_live / test_d9_workflow_lease_live。

**Q5.5 checkpoint 为什么用 SQLite 而不用 PG 表存流程状态？损坏了怎么办？**
解答要点：
- 用 SQLite（SqliteSaver，WAL + busy_timeout）保存 LangGraph checkpoint，进程重启后
  同一文件可 resume；PG 只当业务事实源，两者职责分离。
- 为什么不把 checkpoint 也放 PG：业务事实与流程状态分开，避免 checkpoint 被误当事实
  源（架构图/宪法第五条）；SQLite 单文件适合本地/演示，LangGraph 生态原生支持。
- 损坏处理：SqliteSaver 读损坏文件抛 sqlite3.DatabaseError → 恢复方 **fail-fast**，
  不得静默用损坏流程状态继续；连接生命周期由调用方持有，可显式 close_sqlite_checkpointer。
- 面试诚实点：SQLite checkpoint 是"单机可恢复"语义；多副本/高可用不在 V1 范围
  （PG 的 workflow_threads 提供跨进程推进保护，但 checkpoint 文件本身仍需运维考虑）。

**Q5.6 为什么不用 Celery/任务队列来跑这个工作流？如果要上生产你会怎么选？**
解答要点：
- 本项目的"等待"是**人参与的审批/澄清**（分钟~天、可无限期），不是可重试的短任务；
  Celery 的 ack/retry/visibility timeout 模型不匹配"挂起等人 + 幂等恢复"。
- LangGraph 图内 interrupt + checkpoint + 领域重读把"状态机、人机交互暂停点、幂等恢复"
  统一建模，这正是本项目要展示的 Agent 可靠性语义。
- 若上生产：可将"执行外部退款"这种**短副作用**抽成队列任务（仍要幂等键 + 原键对账），
  而审批等待仍留在图状态；两类技术各管一段，不能把 Agent 全流程硬塞进队列。
- 表达要点：先讲清楚两种模型的适用边界，再给"混合"方案——体现架构判断而非堆名词。

---
# 阶段 6：幂等、并发控制与外部未知对账

**学习目标**：彻底吃透三类"可靠性核心"：幂等键语义、数据库并发控制（乐观锁/行锁/唯一
约束/事务）、以及 operation_unknown 只按原键对账的收口语义。这是项目区别于"demo 聊天
机器人"的关键，面试必被深挖。

**重点阅读**：
- `src/domain/idempotency.py`（payload_hash、get_or_reserve/commit/release、per-key 锁）
- `src/domain/after_sales/pg_commands.py`（幂等记录写入、_op_cas/_ticket_cas）
- `src/repo/interfaces.py` + `src/repo/postgres.py`（UniqueViolation/OptimisticLockError/
  FOR UPDATE 行锁语义）
- `src/repo/schema.sql` 幂等/审批/workflow 段注释
- `tests/unit/domain/after_sales/test_idempotency_concurrency.py`、
  `test_order_refund_concurrency.py`、`tests/regression/test_defect_ledger_regressions.py`
  （D1-D12 全表）
- `docs/INTERVIEW_OVERVIEW.md` §1 时序图末段 + §3 不变量表

**核心概念与术语**：幂等键；同键同载荷返回原结果 / 同键异载荷拒绝；pending 占位；
payload_hash（sha256(json sort_keys)）；原子 check-then-act（CAS）；乐观版本；
唯一约束兜底；订单级 FOR UPDATE；expected_version；D2/D4/D5/D6/D7/D9/D10/D11；
operation_unknown；原 operation_id 对账；禁止换键重试；审计只追加。

**面试问题与解答要点**：

**Q6.1 你的幂等是怎么设计的？重复点击/网络重发为什么不会造成重复退款？**
解答要点：
- 键 = 三元组 (tenant_id, command_type, raw_key)，唯一约束在数据库；请求方传 idempotency
  key，同键同载荷（payload_hash 一致）→ 返回首次结果；同键异载荷 → 拒绝（D2）。
- 内存实现：per-key 锁 + get_or_reserve 建立 `<pending>` 占位再 check-then-act，
  成功后 commit 真实指向，失败 release 占位——占位防止并发窗口内双创建。
- PG 实现：唯一约束（UniqueViolation→稳定错误/返回原记录）+ 单事务；写失败整体回滚。
- 不这样做会怎样：网络重发/用户双击/网关 retry 会创建两张退款单，两次执行 = 双倍退款
  副作用——这是"重复副作用必须为 0"的阻断级不变量（§3 表 + D2 回归）。
- 面试细节：能说出"幂等键必须由客户端/调用方在首次请求生成并透传，服务端不自动生成"
  与"同键不同载荷必须拒绝而不是覆盖"，体现想清楚了语义。

**Q6.2 乐观锁（版本 CAS）和行锁分别用在哪些写路径？为什么不能只用一个？**
解答要点：
- 版本 CAS：approve/reject/execute/reconcile 等**单行推进**用 `UPDATE ... WHERE
  version = expected`；冲突抛 DECISION_VERSION_MISMATCH。防止两个审批人对同一版本提交
  矛盾决定（approval UNIQUE(operation,decided_version) 也兜底）。
- 行锁 FOR UPDATE：execute/reconcile 时对 **orders 行**加锁，串行化"累计已执行退款 + 本次
  ≤ paid_amount"的容量判断——因为容量是跨 operation 的聚合，单靠行版本锁不住另一行
  操作的并发扣款。
- 为什么不能只用一个：版本锁保护"同一行的写冲突"，容量锁保护"跨行聚合一致"；业务
  上两类冲突并存（并发批同一操作 vs 并发执行不同操作抢同一订单额度）。
- 事务边界：读最新事实 → 校验 → 写必须同一事务内完成，锁随事务提交/回滚释放；
  否则 check-then-act 有窗口（D6/D7 回归覆盖）。

**Q6.3 operation_unknown 是什么？为什么"只能按原键对账、禁止换键重试"？**
解答要点：
- 场景：execute 调用外部（模拟）退款超时/结果不明 → operation 落 operation_unknown
  （金额视为 0，无盲目副作用）。
- 正确收口：reconcile 只允许以**原 operation_id** 查询对账（external_result ∈
  {success,...}），对账成功后按结果落账（executed/金额）并关单；**新键被拒**，不自动
  重试写副作用（D10-D11）。
- 为什么不换键重试：外部可能已经成功扣款/退款，只是响应丢失；换新键重试 = 第二笔
  真实副作用 → 重复退款或重复支付。必须"只查原键，确认后再落账"。
- 与真实系统类比：等价于支付回调对账/幂等查询（按原 transaction_id），这是后端工程
  通用可靠性模式——面试要能把这层抽象讲出来。
- 回归：demo 场景⑤（unknown → 原键对账 success → executed/100.00）+ D10/D11 台账。

**Q6.4 内存版与 PG 版在并发语义上有哪些"真实差异"？为什么内存测试不能替代 PG live？**
解答要点：
- 内存：单进程 threading 锁（per-key / per-order RLock），protect 单进程内竞态；
  没有跨进程/跨实例一致性与崩溃恢复。
- PG：唯一约束、行锁、事务回滚、租约表提供跨进程语义；schema CHECK 兜底非法状态。
- 因此 44 个 PG live 用例（integration/phase4/regression 的 live 文件）必须真实连隔离
  库跑（443 全绿），内存跑 399 的 skip 项**不代表 PG 已验证**（TESTING_BASELINE 明示）。
- 面试诚实点：能说出"我的并发正确性结论在 PG live 层验证，内存只是开发/契约测试"。

**Q6.5 审计为什么要做成"只追加不可变"？存哪些字段？日志里会泄露隐私吗？**
解答要点：
- audit_events：tenant/action/entity_type/entity_id/actor/before/after/idem_key/note/
  created_at，只 INSERT 不 UPDATE/DELETE；actor 来自认证 principal（不能伪造）。
- 为什么只追加：审计是合规与追溯证据，可被篡改的审计等于没有审计；追加式 + DB 权限
  限制写入口（只有领域命令路径写审计）。
- 隐私：审计不含完整 PII（手机/邮箱在进入日志前脱敏，见 src/platform/redact.py；
  logging_config 保证任何日志路径不打印完整 PII）。
- 与审批关联：approval_decisions 记 decided_by（认证身份），任何"审批人"动作可追溯到
  具体 principal——面试讲安全合规时这是最强论据之一。

---
# 阶段 7：本地 RAG：政策检索、评分与注入防护

> 模板领域词映射：模板的"ICD 编码检索与评分机制"在本项目 = **PolicyStore 混合检索
> （关键词倒排 + 词频向量 RRF）+ citation 校验 + 注入双向防护**。"医疗隐私"对应本项目
> 的"多租户政策 + PII 脱敏"（阶段 13 展开）。模板若问 ICD/诊断码，如实说明本项目检索
> 的是售后政策文档而非医学编码，但评分/防注入的工程思路可平移。

**学习目标**：能讲清检索链（文档注册→分块→倒排+向量→RRF→引用校验→注入防护→无证据
转人工），并反复强调"检索只给证据，不裁决金额/资格"这一边界。

**重点阅读**：
- `src/rag/store.py`（PolicyDocument/EvidenceChunk/citation/PolicyStore/search/
  validate_citation/detect_injection/_INJECTION_PATTERNS/_poisoned）
- `src/rag/vector.py`（VectorBackend 协议、SparseVector、DictCosineVectorBackend；外部
  向量库按协议接入是规划）
- `src/agents/nodes.py` gather_evidence 中 policy_store 分支（检索如何进 evidence_refs）
- `src/agents/toolkit.py` RetrievePolicy 工具
- `src/api/agent_lab.py` retrieve_policy（工作台真实检索 + blocked/no_evidence 分支）
- `tests/unit/rag/test_rag_store.py`、`tests/unit/agents/test_policy_evidence_single_agent.py`
- `scripts/demo_rag_policy.py`；`evals/rag_metrics.py` 报告口径
- `evals/reports/golden_v1_report.md` / `golden_v2_report.md`（引用正确率三分类口径）

**核心概念与术语**：确定性证据检索基线；PolicyStore；PolicyDocument（tenant/version/
active）；分块（句子切分+max_len 合并）；tokenize（英文/数字词 + 中文 bigram）；
倒排索引 + 词频向量余弦；RRF 混合（reciprocal rank fusion）；citation 格式
`policy_id@version#seq`；validate_citation（存在/启用/版本有效/同租户）；提示注入
（查询注入→拒绝检索；文档内容投毒→排除+记录 _poisoned）；无证据→NO_EVIDENCE→转人工；
检索结果只进 evidence_refs/order_summary.policy_citations，不裁决金额资格；引用正确率
只统计"期望命中现行版"，安全拒绝与注入单列（三分类口径）。

**面试问题与解答要点**：

**Q7.1 你的本地 RAG 检索是怎么做的？为什么不用向量数据库/Embedding？**
解答要点：
- 实现：轻量分词（英文数字词 + 中文 bigram）→ 关键词倒排 + 词频稀疏向量余弦 → RRF
  融合 → 返回带 score/keyword_hits 的块；citation = `policy_id@version#seq`。
- 为什么不用外部向量库：数据规模小（合成政策文档几十条），关键词+词频已足够；零外部
  依赖、可复现、无 embedding 模型成本；`VectorBackend` 协议已预留 pgvector/Milvus 接入
  （规划），不是"不能"而是"当前没有收益不引入"（宪法第十条）。
- 取舍表达：RAG 在这个项目里的角色是"可追溯的证据引用"，不是"语义泛化搜索"；对政策
  这种高精确率要求场景，关键词命中的可解释性反而优于黑盒 embedding。
- 若被追问"中文检索不怎么做 embedding 召回更准吗"：承认 embedding 在语义改写/同义表达
  上更强，但需向量库与评测收益；当前 golden 引用正确率 1.0 已达标，未来按 VectorBackend
  协议替换即可，属于可插拔点。

**Q7.2 citation 为什么带版本？validate_citation 校验什么？**
解答要点：
- 政策会版本更替（P-DAMAGED-FULL@1 旧 / @2 新）；RAG 检索到"词面命中但版本过期/未来/
  不适用"的块时，绝不能当现行政策采信。
- citation = policy_id@version#seq 精确定位"哪一版政策的第几块"，进入
  evidence_refs/order_summary.policy_citations 供展示与复审。
- validate_citation 要求 citation 指向的块：存在、文档启用、版本有效、且**同租户**；
  无效引用被 RiskReviewer 阻断（RISK_CITATION_INVALID）或上层忽略。
- 评测口径三分类：期望命中现行版（计引用正确率）；命中旧版/不适用 → 安全拒绝（单独
  统计，不算正确率下降）；注入 → 拒绝（单独统计）。golden_v1/v2 报告引用正确率 1.0。
- 不这样做会怎样：检索词面命中旧版"30 天全额"政策，Agent 拿旧政策当依据 → 金额错误
  或资格误判；citation 不带版本则无法审计"当时依据的是哪版"。

**Q7.3 提示注入防护在检索侧怎么实现？"文档投毒"与"查询注入"有什么区别？**
解答要点：
- 查询注入：用户 query 命中 `_INJECTION_PATTERNS`（忽略以上/你现在是/扮演/…保守名单）
  → 抛 InjectionDetected → 安全拒绝检索，**不返回任何文档**，工作流转人工（节点返回
  POLICY_INJECTION_DETECTED）。
- 文档投毒：注册/检索到的文档内容本身含注入模式 → 该 chunk 被排除（_poisoned 记录），
  不进 evidence；注入文档无法借检索进入模型上下文。
- 为什么保守名单宁可误伤不可放行（注释原话）：注入一旦放行可能改写 Agent 行为；
  误伤最多转人工，放行则可能越权。
- 纵深：即便检索放行，模型内容守卫（阶段 8）仍拦截金额/审批/执行指令；用户输入、
  RAG 文档、工具返回都视为不可信数据（宪法第五条），不能改变宪法。
- 面试加分：主动讲"检索→证据→模型上下文→写路径"每段都有守卫，单点失守不致命。

**Q7.4 RAG 检索结果为什么不直接决定退款资格/金额？那它到底有什么用？**
解答要点：
- 边界：检索只产出"证据引用与解释"（展示给用户/审批人看：依据 P-DAMAGED-FULL@2#0）；
  真实资格与金额来自领域 `compute_refund_plan`（版本化 PolicyRule 行 + refund_ratio
  计算），PolicyRule 才是"裁决依据"，PolicyDocument 只是"给人读的政策文本"。
- 为什么不直接裁决：文档可能过期/版本错/被注入；政策资格是资损敏感事实，必须走
  DB 里的结构化规则 + 金额计算；检索不可靠时上层转人工而不是猜测（no_evidence 分支）。
- 类比：RAG = 律师引用法条给客户看；法务裁决 = 法院按现行有效法条判案。检索可以找错
  条文，但判案不能错——所以裁决在领域层，引用只是证据。
- 面试确认点：能在时序图里指出"RAG(search+注入检测) → (仅证据引用) → 领域 compute
  refund plan"，与 INTERVIEW_OVERVIEW §1 一致。

**Q7.5 检索"无证据/证据不足"时 Agent 会怎样？为什么空结果也是有效结果？**
解答要点：
- PolicyStore.search 无命中 → 返回空（不是异常）；gather_evidence 把
  summary["policy_notes"]=["NO_EVIDENCE"]，evidence_refs 无 policy 引用；随后领域
  plan 若无适用 PolicyRule → POLICY_NOT_FOUND → escalate 转人工，**不虚构政策/金额**
  （demo 场景③）。
- 为什么空结果重要：Agent 最常见的失败是"证据不足时编答案"；把"空证据"建模为
  显式状态并路由到转人工，是把"不会就说不会"变成流程语义。
- Agent Lab retrieve_policy 也区分 status=ok / no_evidence / blocked 三种返回，工作台
  与测试都断言零副作用。

---
# 阶段 8：LLM 建议层：能力矩阵、内容守卫与降级

**学习目标**：吃透 `src/models/` 的整条链：配置只读环境变量 → 客户端统一协议 → 能力矩阵
→ 结构化 Schema → Prompt 版本 → 内容守卫 → 降级链 → 影子评测；准确说出"真实 LLM 未
实测"的事实与设计如何保证未实测也不危险。

**重点阅读**：
- `src/models/base.py`（LLMClient 协议、异常体系、assert_output_safe 内容守卫）
- `src/models/config.py`（仅环境变量读 OPSPILOT_LLM_*、白名单、Key 校验、零日志）
- `src/models/router.py`（ModelRegistry/ModelGateway/能力矩阵/降级链）
- `src/models/offline.py`（OfflineRuleClient 规则基线）
- `src/models/openai_compatible.py`（HTTP 适配器与安全降级细节）
- `src/models/schemas.py`（结构化输出 Schema）、`src/models/prompts.py`（PROMPT_VERSION）
- `src/agents/modes.py`、`scripts/demo_modes.py`、`scripts/gen_sft_samples.py`（微调样本入口）
- `docs/MODEL_EVALUATION.md`（全文）、`evals/run_model_shadow_eval.py`
- `evals/reports/shadow_eval_offline.md`

**核心概念与术语**：能力矩阵（6 项：4 低风险默认启用 + tool_calling/high_risk_draft 高
风险默认禁用）；降级链 primary→offline→转人工信号；ModelConfigError（构造即失败、零网
络）；ModelTimeout/Network/RateLimited/Http/Parse/Schema/Quota/ContentPolicy 错误；
assert_output_safe（金额/审批/状态/执行词拦截，先于解析）；LLMClient 统一协议；
Prompt 版本化（改 Prompt 必须升版本 + 重跑黄金集）；PII 发送前脱敏；零网络兜底；
offline 规则意图准确率 1.0（golden-v1）；候选模型"未实测/N/A"；能力矩阵对微调模型同
样生效；微调门禁（无对照数据禁止 LoRA/QLoRA/DPO）。

**面试问题与解答要点**：

**Q8.1 你的"LLM 建议层"边界是什么？模型到底能做什么、不能做什么？**
解答要点：
- 能：低风险语言任务——意图识别、缺参字段抽取、澄清话术、基于已验证证据的解释
  （intent_classification/structured_extraction/clarification_copy/evidence_explanation）。
- 不能：tool_calling（模型自主选工具）与 high_risk_draft（高风险草稿）默认**禁用**
  （能力矩阵返回空/抛 ModelContentPolicyError）；任何模型输出都只是"语言结果"，
  不能直接写业务表、不能调写工具。
- 为什么这样切：模型的价值在语言理解/生成；业务裁决在领域层。能力矩阵把"哪些任务
  允许模型参与"显式化、可测试，防止未来接真模型时默认放权。
- 面试自检：能说出 6 个 ModelTask 名字 + 高低风险分组，能说出高风险任务被禁用后
  Gateway 的返回路径（抛异常/返回空）而不是含糊"模型不能用"。

**Q8.2 内容守卫（assert_output_safe）拦截什么？为什么要在解析前先拦？**
解答要点：
- 拦截模型输出文本中的金额（¥/元/rmb/数字+元）、审批/状态/执行指令（批准/审批通过/
  执行退款/approve/execute/refund now/close ticket…）、状态迁移词——正则 + 动作词表
  双覆盖中英文。
- 在解析前守门：模型若在回答里写了"同意退款 500 元"，先被守卫拒绝（ModelContentPolicyError）
  → 降级/转人工，避免"结构化字段侥幸通过但文本里夹带越权语义"。
- 四角色多 Agent 也有同语义本地轻量守卫（multiagent.assert_agent_copy_safe +
  assert_agent_payload_safe 递归检查嵌套 dict/list），且 Resolution schema **无金额字段**
  ——双层：schema 不让有 + 文本守卫不让写。
- 不这样做会怎样：模型文本可携带隐式指令流向下游；金额若由模型决定则幻觉直接变资损。
- 面试表达：内容守卫是"纵深防御"的一环——矩阵管任务、schema 管结构、守卫管文本、
  领域服务管最终裁决，四层缺一不可。

**Q8.3 未配置 API Key 时系统如何行为？"安全降级"具体链路是什么？**
解答要点：
- config：仅从环境变量 OPSPILOT_LLM_API_KEY / OPSPILOT_LLM_BASE_URL 读取，URL 必须 ∈
  白名单（OPSPILOT_LLM_ALLOWED_BASE_URLS 或默认白名单）；缺 Key/URL 非法 → ModelConfigError，
  在构造 OpenAICompatibleClient 时即失败，**零网络请求**。
- modes.resolve_mode("llm")：尝试加载设置 + assert_safe_network，失败 → 回落
  OFFLINE_RULE（不静默直连）；demo_modes 演示这条路径。
- ModelGateway.run_task：主模型存在且矩阵启用 → 调用；抛 ModelError → 降级到
  OfflineRuleClient 并标记 metadata.degraded=True + error 原因 + provider 保持离线；
  无主模型 → 直接离线（非降级）。
- 结果：任何时刻系统都能跑（离线规则），且报告明示 "未实测/N/A"，绝不把离线当真实
  模型结论（MODEL_EVALUATION §4 规则）。
- 面试话术：没有 Key 也能演示全部闭环——这正是"可降级设计"的价值；接 Key 是配置
  而非代码改动。

**Q8.4 影子评测是什么？为什么要做影子而不是直接切换主模型？**
解答要点：
- 影子：业务仍走离线规则（不接模型）；候选模型对同一输入做预测但**零业务副作用**
  （不建工单/不写审计/不驱动审批；专项测试断言领域状态不变），对照 离线/候选/黄金期望。
- 为什么影子：真实模型行为未知（延迟/幻觉/格式漂移），直接切换会把未知风险带入生产
  链路；影子先收集失败样本与差异指标，验证后再决定是否放行。
- 报告必填字段：模型名与版本、Prompt 版本、数据集版本、运行时间、Token、成本、耗时、
  错误率、降级数（ModelInvocationMetadata 记账：duration/tokens/cost_estimate/degraded/
  error）。
- 未配置 Key 时 candidate 模式不联网、报告标注未实测——诚实边界贯穿评测工具。

**Q8.5 微调（LoRA/SFT/DPO）为什么不做？什么条件下才会做？**
解答要点：
- 现在不做：无高质量 chosen/rejected 数据集、无真实 LLM 失败样本集、无对照指标；
  动态价格/政策/库存是 DB 事实不能写进模型参数（宪法第八条）；`gen_sft_samples.py`
  只是固定种子**样本生成入口**，无 GPU/Key 未训练，不声称任何效果。
- 条件：先跑同黄金集/影子集保存对照报告 → 有可量化基线 → 才允许 LoRA/QLoRA/SFT；
  DPO 需质量可控的偏好对；微调模型**不得**获得额外写权限，能力矩阵与内容守卫同样生效。
- 面试升级：能引用宪法第八条并讲出"先 Prompt/RAG/结构化输出基线，再谈微调"的顺序，
  说明你懂什么时候微调才有意义（固化稳定格式/话术，而非记忆动态事实）。

---
# 阶段 9：运行模式与四角色多 Agent 实验

**学习目标**：讲清四种 RuntimeMode、resolve_mode 回落逻辑、SupervisorRunner 的
three-agent 与 four-role 编排、四角色职责与只读白名单、RiskReviewer 阻断项、trace
结构，以及 A/B 无收益→默认单 Agent 的完整论证。

**重点阅读**：
- `src/agents/modes.py`（RuntimeMode 四值 + resolve_mode）
- `src/agents/supervisor.py`、`src/agents/subagents.py`（三只读子 Agent spec）
- `src/agents/multiagent.py`（四角色 Schema/角色/白名单/RiskReviewer/编排节点）
- `src/agents/runner.py`（SupervisorRunner 与 WorkflowRunner 的 API 一致性）
- `scripts/demo_supervisor_trace.py`（8 场景 trace 输出）
- `evals/compare_agents.py`、`evals/compare_modes.py`（A/B 对照实现）
- `docs/MULTI_AGENT_EXPERIMENT.md`、`evals/reports/agent_compare.md`、
  `evals/reports/mode_compare.md`
- `tests/unit/agents/test_multiagent_four_role.py`、`test_modes.py`、
  `test_supervisor_trace_demo.py`

**核心概念与术语**：RuntimeMode（single_agent 默认/multi_agent/offline_rule/llm）；
resolve_mode 回落；SupervisorRunner(orchestration="three-agent"|"four-role")；
Triage → Evidence → Resolution → RiskReview；只读工具白名单 + TenantContext；Resolution
无金额字段；RiskReviewer 阻断 RISK_CROSS_TENANT / RISK_CITATION_INVALID /
RISK_AMOUNT_NOT_FROM_DOMAIN 等；trace（trace_id=ma-{thread}:{agent}，input/output 摘要
脱敏、duration_ms、status、reject_reason、citations、tool_calls），挂在
order_summary.supervisor.traces；子 Agent 失败/证据不足/注入 → escalate 转人工；
A/B 11/11 持平 → 默认单 Agent；"角色契约是未来真实 LLM 子 Agent 的挂载点"。

**面试问题与解答要点**：

**Q9.1 四种运行模式分别是什么？"llm"模式没配 Key 会怎样？**
解答要点：
- single_agent：默认，单 Agent LangGraph（意图/澄清/证据/审批恢复/unknown 对账）。
- multi_agent：四角色只读编排（实验），SupervisorRunner orchestration="four-role"。
- offline_rule：显式声明"无 LLM Key 的离线规则模式"，行为与 single_agent 一致。
- llm：仅当 OPSPILOT_LLM_API_KEY 与白名单 Base URL 都配置才可用；未配置 →
  resolve_mode 捕获配置异常回落 OFFLINE_RULE，绝不静默直连。
- 关键：**模式只影响"意图/澄清/证据编排"的决策来源**；金额/资格/权限/审批/状态永远由
  确定性领域服务裁决并从 PG 重读，任何模式都不能绕过（modes.py 头注释原话）。
- 面试表达：模式开关 = "决策来源可替换，业务护栏不可替换"——这是架构最凝练的一句话。

**Q9.2 四角色各自职责？为什么 Resolution 没有金额字段？**
解答要点：
- Triage：意图标签/订单提示/诉求标签/缺参初判（与 parse 同源规则）。
- Evidence：订单+客户+历史工单+政策检索，产出 evidence_refs/citations（只读工具白名单：
  get_order/list_customer_tickets/retrieve_policy）。
- Resolution：动作草稿意图与说明（action_kind/policy 说明）；**schema 无金额字段**——
  金额唯一来自领域 compute_refund_plan（文本还经内容守卫，双保险）。
- RiskReview：审查引用有效性/租户一致/金额来源/动作一致性/订单状态；命中风险 →
  RISK_* reject_reason，编排转人工。
- 为什么金额不进角色 schema：防止"模型/角色路径上出现金额数值"成为未来真实 LLM 的
  幻觉载体；领域裁决是金额唯一出口（与单 Agent 的 compute_refund_plan 同源）。
- 面试细节：能说出 RiskReviewer 阻断项的具体命名（RISK_CROSS_TENANT /
  RISK_CITATION_INVALID / 金额非领域来源）与 escalate 后的零副作用。

**Q9.3 子 Agent 怎么保证"只读"？谁负责写路径？**
解答要点：
- 工具注册表（src/platform/tooling.py）构造即校验：注册表内不允许非只读工具；每个子
  Agent 有独立工具白名单，白名单外调用 → PERMISSION_DENIED（测试断言）。
- 写命令只在**顶层** Supervisor/Gateway（领域服务 + 人工审批）；子 Agent 无任何写工具。
- TenantContext 强绑定：工具入参不允许带其他租户 id，跨租户尝试一律拒绝；调用记录
  ToolCallRecord 审计。
- 面试说法：多 Agent 的"权力收敛"——子 Agent 再聪明也够不到写通道，写只经顶层
  gateway → 领域服务 → 人工审批。

**Q9.4 trace 记录了哪些内容？为什么摘要要脱敏？**
解答要点：
- trace_id = ma-{thread}:{agent_name}；记录 input_summary/output_summary（截断）、
  duration_ms、status（ok/error/skipped）、reject_reason、citations、tool_calls；
  挂在 order_summary.supervisor.traces（追加 key，不破坏既有 state 语义与测试）。
- 脱敏：手机号/邮箱在摘要写入前掩码（_PHONE_RE/_EMAIL_RE），宪法第五条"日志不得泄露
  完整 PII"；即便 trace 只是展示也先脱敏——日志/可观测数据默认不可信。
- 作用：四角色执行可复现查看（demo_supervisor_trace 8 场景输出完整轨迹），也是未来
  真实 LLM 子 Agent 的可观测性挂载点。
- 面试加分：能把 trace 与"评测/可观测性"串起来：A/B 对照不仅看 outcome，还看角色
  轨迹是否合理。

**Q9.5 你凭什么说"多 Agent 无收益"？这个结论的实验方法严谨吗？有什么局限？**
解答要点：
- 方法：同一 golden_v1（11 条）逐条分别驱动 single / three-agent / four-role，同一
  PolicyStore 数据面、同一安全约束/审批/幂等语义；比较通过率、outcome、退款金额、
  转人工数（escalated single=5/three=5/four=5）；报告为确定性产物（不含逐 run 耗时；
  耗时仅 stdout——D12 回归保障报告可复现）。
- 结果：11/11 全过且 outcome/金额一致 → 无量化业务收益 → 按 ADR-002 回退条款维持
  单 Agent 默认。
- 局限（主动说）：当前所有角色是**确定性规则实现**（不是真实 LLM），"无收益"只在这
  一实现下成立；若未来引入真实 LLM 子 Agent 路由且指标出现差异，需要重跑实验、
  有正收益再切换默认并更新 ADR（MULTI_AGENT_EXPERIMENT §5）。
- 面试升级：能说明"为什么不用耗时当结论指标"（耗时受机器/并发影响、非业务指标，
  曾经误写入报告被 D12 回归纠正——这是失败案例，见阶段 15）。

**Q9.6 四角色与单 Agent 复用同一 LangGraph 图吗？怎么做到的？**
解答要点：
- graph.build_workflow(mode=..., evidence_node=...)：Supervisor 模式只是把
  gather_evidence 节点替换为 supervise_evidence（等价编排），其余节点/状态 Schema/
  interrupt 语义完全一致——所以黄金集 A/B 对照公平（差量只在证据节点）。
- AgentState 语义等价：evidence_refs/order_summary/risk_level 等都产出，便于同一
  评测管线比较。
- 意义：不是两套系统，是"一个图、一个可替换证据节点"——改动面最小、对照最干净。
- 面试表达：架构上"可插拔证据编排"是 SupervisorRunner 与 WorkflowRunner 共用 API 的
  原因，也是未来接真实 LLM 子 Agent 的挂载点。

---
# 阶段 10：API 接入层、认证授权与本地工作台

**学习目标**：讲清 FastAPI 如何只依赖 AfterSalesApplicationPort、认证身份推导与角色门禁、
tenant-first、pg profile 的 no-fallback 启动纪律、健康检查，以及 Agent Lab + 本地 UI
工作台的安全隔离边界。

**重点阅读**：
- `src/api/app.py`（工厂、中间件、全部路由、错误处理注册、/health/live、/health/ready）
- `src/api/deps.py`（AuthMiddleware、TokenResolver、get_identity：X-Api-Key → 本地身份）
- `src/api/errors.py`、`src/api/schemas.py`（稳定错误/响应 Schema）
- `src/api/runtime.py`（memory vs pg profile 启动纪律）
- `src/api/agent_lab.py`（隔离合成沙箱：trace/retrieve/boundary-scenario）
- `src/api/ui/`（index.html/workspace.js/workspace.css——本地演示工作台）
- `scripts/run_api.py`（两种 backend 启动方式与禁止项）
- `tests/e2e/test_api_full_flow_e2e.py`、`tests/unit/api/test_api.py`、
  `test_decision_version_required.py`、`test_agent_lab.py`、`test_health.py`

**核心概念与术语**：身份由认证结果推导（请求体 tenant_id 不被信任）；X-Api-Key → 本地
身份 token registry；角色门禁 agent/customer/approver/system；客户只能读写自己的数据
（D3）；tenant-first（领域调用一律带认证 tenant）；expected_version 必填（pg profile
422，服务端不代填）；/health/live 与 /health/ready（PG 探测 503 degraded）；pg profile
启动失败绝不静默降级 memory；Agent Lab 每次请求新建固定种子内存后端（只读隔离，不改
业务事实）；demo/reset 仅 memory 注册；双入口边界重申。

**面试问题与解答要点**：

**Q10.1 API 层的认证授权怎么做？为什么不信任请求体里的 tenant_id？**
解答要点：
- AuthMiddleware 解析 X-Api-Key → registry 中本地身份（tenant/role/principal/customer_id）
  → request.state.identity；路由 Depends(get_identity) 取用。
- 请求体（TicketCreateIn 等）**不携带 tenant_id**，tenant 一律取认证身份——请求体可被
  客户端伪造，若信它则任意租户可冒用他人租户上下文（跨租户越权）。
- 角色门禁：create_ticket/refund-draft/submit 需 agent/customer；approve/reject 需
  approver；execute/reconcile 需 system；任何角色不符 → AFTER_SALES_PERMISSION_DENIED。
- 客户级授权：CUSTOMER 只能创建/读取**自己 customer_id** 的工单（D3，双保险：
  领域查询 + API 层再校验）。
- 面试升级：能说出"认证（你是谁）→ 授权（你能干嘛）→ 资源级授权（你能碰哪条数据）"
  三层，且每层都有测试。

**Q10.2 pg profile 启动为什么要"no-fallback"？如果 PG 挂了 API 会怎样？**
解答要点：
- runtime.py 纪律：pg profile 必须满足 PostgreSQL 可达 + Alembic 迁移 ≥ 期望版本 +
  健康探测，否则**启动失败并报错**，绝不静默降级到 memory。
- 为什么：静默降级 = 你以为在写 PG（生产事实源）实际写进内存 dict——重启即丢，且
  "唯一事实源"承诺被破坏；宁可启动失败暴露问题，不可带错配置上线。
- /health/ready：配置 pg_probe 时探测失败 → 503 degraded（reason=PostgreSQL 不可用）；
  /health/live 只答 ok（进程活着不代表依赖可用）——区分 liveness 与 readiness。
- 面试表达：no-fallback 是"fail-fast 胜过错误成功"的工程原则在部署层的体现，
  与代码层的 fail-closed（guard）一脉相承。

**Q10.3 Agent Lab 和本地 UI 工作台是什么？它会不会碰"真实业务数据"？**
解答要点：
- Agent Lab（src/api/agent_lab.py）：面向工作台的**隔离合成沙箱**——每次请求新建
  固定种子内存后端（订单 ORD-LAB-1001 + 破损政策），展示真实 单 Agent/Supervisor 轨迹、
  RAG 检索、边界剧本（clarify/no_evidence/cross_tenant/pii）；**不读取或修改 API 正在
  服务的业务事实**（docstring 明示），不执行退款副作用（side_effect=False）。
- UI 工作台（src/api/ui）：中文本地演示页，通过受保护 /api 调领域接口，展示案件处置/
  Agent Lab/评测边界；用合成数据，不能视为生产运营后台。
- 隔离价值：面试现场能"点开看"轨迹与拒绝原因，而不会污染演示数据或泄露任何真实数据。
- 诚实表述：工作台 = 演示与边界展示工具，不是生产前端（STATUS_AND_RISKS 明示）。

**Q10.4 expected_version 在审批接口里为什么必须由客户端提供（pg profile）？**
解答要点：
- 客户端调用 approve/reject 时提交 expected_version（操作当前版本）；pg profile
  require_expected_version=True → 缺失返回 422 EXPECTED_VERSION_REQUIRED，**服务端
  不代填当前版本**。
- 为什么不让服务端兜底：代填 = 把"读-改-写"竞态的责任放回服务端并掩盖并发问题——
  客户端读到 v3，另一人推进到 v4，若服务端用 v4 兜底则旧决定悄悄覆盖新事实。
- 正确语义：expected_version 是客户端"我基于 v3 做决定"的声明；服务端 CAS 校验
  v3==当前 → 通过，否则 DECISION_VERSION_MISMATCH。memory 演示保留宽松兜底但 pg
  生产语义强制。
- 对应测试：test_decision_version_required + phase4 red contracts。

**Q10.5 API 返回的错误码/错误结构为什么不直接暴露内部异常？**
解答要点：
- register_error_handlers 把领域 AfterSalesErrorCode 映射到稳定 HTTP 结构（code/message），
  不是把 Python traceback/内部类名吐给客户端。
- 理由：客户端与上层 Agent 需要可编程的错误码（转人工分支依赖 POLICY_NOT_FOUND 等），
  且不泄露内部实现细节（安全）；错误码与领域枚举一致，保证 API 断言 = 领域断言。
- 面试细节：跨租户读在 memory=403 TENANT_MISMATCH / PG=404 NOT_FOUND 的差异是有意
  的（不泄露资源存在性），要能解释为什么允许这种"不一致"。

---
# 阶段 11：评测体系：黄金集、回放、A/B 对照与影子评测

**学习目标**：能讲清评测资产（golden_v1/v2）、回放命令、三份对照/报告的口径、引用
三分类统计、报告"确定性/可复现"约束，以及 memory 与 pg profile 回放的差异。

**重点阅读**：
- `evals/replay.py`（--dataset golden_v1/golden_v2；--profile memory/pg；pg 装配链）
- `evals/compare_agents.py`、`evals/compare_modes.py`（A/B 与三模式对照）
- `evals/rag_metrics.py`（引用三分类指标）
- `evals/run_model_shadow_eval.py`（offline/candidate 影子）
- `evals/reports/`（golden_v1_report/golden_v2_report/agent_compare/mode_compare/
  shadow_eval_offline）
- `tests/unit/evals/`、`tests/unit/evals/test_citation_metrics.py`
- `docs/TESTING_BASELINE.md`、`docs/MODEL_EVALUATION.md`

**核心概念与术语**：黄金集 golden_v1（11 条）/ golden_v2（120 条）；固定种子合成数据；
memory profile（默认基线，golden_v1 11/11、golden_v2 120/120）；pg profile（真实 PG
事实源回放：不可达/schema≠0006 → RuntimeError，绝不回退 memory；每用例重建业务表）；
引用正确率只统计"期望命中现行版"，安全拒绝与注入单列（三分类口径）；报告确定性产物
（不含逐 run 耗时；耗时仅 stdout——D12）；AgentState 语义等价对照；shadow eval
（offline 已实测 1.0；candidate 未配 Key 标未实测）；"没有真实运行并保存报告，不得声称
真实模型已验证"。

**面试问题与解答要点**：

**Q11.1 你的黄金集评测是怎么设计的？golden_v1 和 golden_v2 各测什么？**
解答要点：
- golden_v1（11 条）：核心闭环 + 安全场景——正常退款/审批拒绝/缺订单澄清/无政策转
  人工/未知意图/外部 unknown 对账/重复请求幂等/伪造恢复安全等，每条带期望 outcome/
  金额/错误码。
- golden_v2（120 条）：扩展覆盖（120/120），含政策版本探针、注入样本等；同口径统计
  （引用正确率仅计期望命中现行版；旧版/拒绝/注入单独计）。
- 评测 = 确定性回放（replay.py）：每用例 start → （若需审批）自动提交决定 → resume →
  断言 outcome/金额/安全不变量；不是 LLM 评分，是可复现的业务断言。
- 数据边界：固定种子合成数据（seed 42），不代表真实企业收益（宪法第二条）——报告
  都要标注。
- 面试加分：能解释"黄金集断言业务终态与安全不变量，而不是只比'回答像不像'"——这是
  与普通 LLM eval 的本质区别（宪法第七条：确定性检查优先验证最终业务状态/金额/权限/
  引用版本/重复副作用）。

**Q11.2 memory 与 pg profile 回放有什么不同？为什么 pg 失败要硬失败而不是回退？**
解答要点：
- memory：内存 AfterSalesService + MemorySaver checkpoint + 模拟外部执行——快、无依赖，
  是既有基线（11/11、120/120）。
- pg：装配链 = require_postgres_ready(url)（不可达/schema≠0006 → RuntimeError）→
  每用例重建全部业务表（schema.sql）→ PostgresAfterSalesRepository → PgCommandService
  → PgCommandAdapter（完整 Port）→ open_sqlite_checkpointer（临时唯一文件）。
- pg 失败不回退 memory：回退会让"PG 验证"变成"内存冒充 PG 验证"——报告会撒谎；
  硬失败才能暴露环境问题（同理见 Q10.2 no-fallback）。
- 面试细节：能说出 pg 回放会重建 schema（所以必须 guard 指向 opspilot_test_* 隔离库），
  并把 replay 的 reset_schema 与 guard 绑定这点讲出来。

**Q11.3 RAG 的"引用正确率 1.0"是怎么算的？会不会是"刷分"？**
解答要点：
- 三分类口径（关键）：① 期望命中**现行版**（如 query 破损 → 期望 P-DAMAGED-FULL@2）
  → 计入引用正确率；② 词面命中旧版/不适用版本（P-DAMAGED-FULL@1）→ 算"安全拒绝"，
  **不计入**引用正确率分母（正确拒绝旧版不是召回失败）；③ 注入 → 单独计拒绝。
- 为什么这样统计：旧版拒绝是"安全行为"不是"检索错误"；若把②当失败，会惩罚正确的
  安全设计，评测口径必须与安全语义对齐。
- 防刷分：golden_v2 特意放版本探针与注入样本（120 条含这些负样本），同口径统计，
  期望"正向命中正确 + 负向拒绝正确"双达标，而不是只追求一个号。
- 面试表达：评测口径要能解释"为什么旧版命中不算错误"，体现你设计指标时想清了
  业务语义而非照搬 accuracy 公式。

**Q11.4 对照报告为什么不能含运行耗时？D12 是怎么防复发的？**
解答要点：
- 结论只用确定性指标（通过率/outcome/金额/转人工数）；耗时受机器负载/并发影响、
  非业务指标，曾误入报告被 D12 回归（test_d12_reports_stable_no_wallclock_jitter）
  纠正——报告不含 "ms" 列，耗时只打到 stdout。
- 为什么重要：报告是仓库内可复现产物，若含墙钟时间则两次运行 diff 不一致，
  无法做"报告稳定"断言，也无法作为证据提交。
- D12 是失败案例的好素材：说明作者会"用回归测试锁住自己犯过的错"（详见阶段 15）。
- compare_modes 输出可复述：single 11/11、three 11/11、four-role 11/11，escalated
  single=5/three=5/four=5，online_llm=not_configured，引用探针 2/2 一致。

**Q11.5 评测报告与测试的边界在哪？"评测通过"能不能替代"测试通过"？**
解答要点：
- 测试（pytest）：锁实现细节与不变量（单元/集成/安全/property/regression）——
  保证"代码改坏能发现"。
- 评测（evals）：锁**端到端业务期望**（黄金集回放 outcome/金额、A/B 对照、影子
  指标）——保证"系统对真实业务剧本表现符合预期"。
- 边界：评测依赖测试保证的底层正确性；测试不覆盖"整条链路对黄金剧本的表现"；
  两者互补，报告都进 evals/reports + TESTING_BASELINE 记录。
- 面试说法：先分层测试把"组件对"做扎实，再用黄金集把"端到端剧本对"锁住，
  任何 Prompt/规则/DB 变更都要重跑同一套（prompts.py 强制：改 Prompt 升版本 + 重跑）。

---
# 阶段 12：测试策略、PG 隔离 guard 与 CI

**学习目标**：能讲清测试分层与数量口径（399/443/25×2）、PG live 为何需要 guard、
guard 的 fail-closed 规则、隔离双跑脚本、CI 内容与它的真实边界（CI 里 PG live 是
skip 的），以及 D 盘环境纪律。

**重点阅读**：
- `docs/TESTING_BASELINE.md`（模式 A/B 口径、隔离双跑、分层目录说明）
- `tests/pg_live.py`（live 测试统一入口与 skip 条件）
- `src/platform/pg_test_guard.py`（is_allowed_test_db_url/reject_reason/
  require_isolated_test_db/reset_test_schema/test_database_url_from_env）
- `scripts/run_pg_tests_isolated.ps1`（隔离双跑脚本）
- `.github/workflows/ci.yml`（注意头部注释与 run #1 实测边界）
- `scripts/run_tests.py`（分层回归入口）
- `scripts/init_d_env.ps1`、`src/platform/runtime_paths.py`（D 盘约束）
- 测试目录：tests/unit（domain/agents/models/rag/api/platform/evals/repo/persistence）、
  integration、e2e、property、security、regression、phase4

**核心概念与术语**：内存模式 A（399 passed/44 skipped/1 warning）vs 隔离 PG 模式 B
（443 passed/0 skipped/1 warning）；44 个 skip = 未配置/不可达隔离 PG 的 live 用例
（**不代表 PG 已验证**）；PG guard fail-closed（host ∈ localhost/127.0.0.1 且库名
opspilot_test_*；共享库 opspilot/postgres/template* 拒绝；畸形 URL 拒绝；非法 URL 在
任何连接探测前失败）；破坏性目标唯一来源 OPSPILOT_TEST_DATABASE_URL（不回退
DATABASE_URL）；reset_test_schema（DROP 业务表 + 重放 schema.sql，仅合法隔离库）；
隔离双跑（每 run 两个唯一库 opspilot_test_{a,b}_{guid}，各 25 passed，迁移至当前 head 0006）；
CI（push bcf3fe8/2db03e3 → run #1 success：alembic upgrade + run_tests.py 分层回归；
CI 未设 OPSPILOT_TEST_DATABASE_URL → PG live 在 CI 中 skip）；分层测试
unit/integration/e2e/property/security/regression/phase4；D 盘约束（TEMP/PIP cache/
pycache 重定向 .runtime/.cache，禁止写 C 盘）。

**面试问题与解答要点**：

**Q12.1 你的测试怎么分层？每层防什么？数量口径如何复现？**
解答要点：
- 分层：unit（领域规则/Agent 节点/模型/平台工具/API 单测）、integration（PG/API 并发/
  租约/行编解码 live）、e2e（API 全流程走 HTTP：审批/权限/幂等）、property（退款不变量
  性质测试）、security（阻断级安全不变量：越权/注入/PII）、regression（缺陷台账
  D1-D12）、phase4（PG profile/租约 live）。
- 口径：模式 A（不设测试库 env）→ 399 passed/44 skipped（PG live 按纪律 skip）；
  模式 B（OPSPILOT_TEST_DATABASE_URL 指向 opspilot_test_* 隔离库）→ 443 passed/
  0 skipped——**44 个 skip 只表示该运行模式未启用 PG live，不能写"PG 已验证"**。
- 隔离脚本：run_pg_tests_isolated.ps1 每 run 建两套唯一库（a/b + guid）从空库 alembic
  upgrade head 各跑 25 passed（迁移到 0005），再在其中一套跑全量 443。
- 面试表达：能随口报出 399/443/25×2 与它们的含义差异，是"我真跑过"的硬证据。

**Q12.2 PG 破坏性测试为什么需要 guard？guard 拒绝哪些 URL？**
解答要点：
- 破坏性 = DROP 全表 + 重建 schema；若指向共享开发库/主库 = 数据灾难。所以要求
  fail-closed：**只允许** host ∈ {localhost,127.0.0.1} 且库名以 opspilot_test_* 开头
  的显式测试库。
- 拒绝分类：非本机 host（db.example.com/生产域名/内网 IP）、共享/系统库名
  （opspilot/postgres/template0/template1）、非 opspilot_test_* 前缀、缺库名/畸形 URL；
  且**非法 URL 在任何连接探测前直接失败**（有专门测试验证不先连库）。
- 目标唯一来源：OPSPILOT_TEST_DATABASE_URL，**不回退** DATABASE_URL——防止有人设了
  DATABASE_URL 指向共享库就把破坏性测试打到上面。
- reset_test_schema：guard 拒绝时抛 TestDbGuardError 且不创建连接/不执行 SQL；
  合法时真实 DROP（表清单从 schema.sql 动态提取 10 张业务表）+ 重放 schema.sql。
- 面试加分：guard 是"测试也要安全的架构"——测试代码同样遵循最小权限，不是想 drop
  就 drop。

**Q12.3 CI 里到底跑的是什么？它的结果能证明 PG 已实测吗？**
解答要点：
- ci.yml：push/pull_request 触发 → ubuntu + python3.12 + postgres:16-alpine 服务 →
  pip 安装（清华镜像）→ alembic upgrade head → scripts/run_tests.py（分层 + 全量回归）。
- run #1（push bcf3fe8，2026-09-06）job test (3.12) 各步骤 success——CI 全绿。
- **诚实边界**：CI 只设 DATABASE_URL（共享库 opspilot），未设 OPSPILOT_TEST_DATABASE_URL
  → 44 个 PG live 破坏性用例在 CI 中按 guard 纪律 **skip**（等同 399 passed 口径），
  CI 的 success 不代表 PG live 443 已在远程实测；443 由本地隔离双跑/模式 B 实测。
  ci.yml 头部注释已如实写明这一点。
- 面试若被问"CI 挂了你的 PG 测试怎么办"：答"CI 验证分层回归与迁移；PG live 由本地
  隔离库双跑脚本负责，两条线分开，guard 保证远程也永远不会误打共享库"。

**Q12.4 D 盘环境约束（init_d_env.ps1 / runtime_paths.py）解决了什么真实问题？**
解答要点：
- 问题：pip 缓存/pytest 临时目录/Python 字节码/checkpoint 默认写 C 盘用户目录；中文路径
  下 pytest cache 写失败（R2）；项目要求"所有临时与缓存不落 C 盘、不污染系统"。
- init_d_env.ps1：仅当前进程设 TEMP/TMP/PYTEST_DEBUG_TEMPROOT/PIP_CACHE_DIR/
  PYTHONPYCACHEPREFIX 到项目 .runtime/ 与 .cache/（不改系统变量；脚本 ASCII-only + UTF-8
  BOM 兼容 PS5.1）。
- runtime_paths.py：统一运行时路径（默认 .runtime/，OPSPILOT_RUNTIME_ROOT 可覆盖），
  让 run_api/replay/demo 的临时文件显式落 D 盘。
- 面试价值：能讲出"存储约束→CI/本地可复现→不污染用户机器"的工程细节，说明你处理过
  真实环境问题而非只写业务代码。

**Q12.5 如果 CI 加了 OPSPILOT_TEST_DATABASE_URL 指向 CI 的 postgres 服务，会发生什么？**
解答要点（开放设计题）：
- CI postgres 服务库名是 opspilot（共享名），guard 会拒绝（非 opspilot_test_*）→ live
  仍 skip 或报错。要让 CI 跑 live：需在 CI 里用管理员连接先 CREATE DATABASE
  opspilot_test_ci_xxx，再把该 URL 设给 OPSPILOT_TEST_DATABASE_URL（隔离双跑脚本正是
  这么做的）。
- 换而言之：guard 的规则把"CI 想跑 live"的门槛变成"必须先建合法隔离库"，这是特性
  不是缺陷——防止任何人（包括 CI 配置作者）误把破坏性测试指向共享库。
- 改进方向：CI 增加"建隔离库 → 跑 live"步骤即可让远程也覆盖 PG live；当前诚实标注
  CI 未覆盖。

---
# 阶段 13：安全与合规：审计、脱敏、提示注入、多租户

> 模板领域词映射：模板"医疗隐私/合规"在本项目 = **客户 PII 脱敏 + 多租户隔离 + 审计
> 可追溯 + 提示注入防护 + 审批伪造防护**。医疗行业若追问，可平移"最小权限/审计/脱敏/
> 不可伪造审批"到金融/医疗通用合规语言，但不要虚构 HIPAA 等具体认证。

**学习目标**：能按"认证→授权→资源授权→数据脱敏→审计→注入防护→审批伪造防护"讲完整
安全链，并把每条都对应到测试/回归编号（D1-D12）。

**重点阅读**：
- `src/platform/redact.py`（PII 掩码规则：手机/邮箱/身份证等）
- `src/platform/logging_config.py`（日志不落完整 PII）
- `src/agents/multiagent.py` 摘要脱敏（_PHONE_RE/_EMAIL_RE）
- `src/rag/store.py` 注入模式与投毒排除
- `src/api/deps.py` + `src/api/app.py`（认证/角色/资源授权）
- `src/domain/after_sales/pg_commands.py` + `schema.sql`（decided_by/审计/唯一约束）
- `tests/security/test_blocker_invariants.py`、`tests/regression/test_defect_ledger_regressions.py`
- `docs/INTERVIEW_OVERVIEW.md` §3 安全不变量报告
- `AGENTS.md` 第五、六条

**核心概念与术语**：安全不变量六项（越权/重复副作用/换键重试/非法迁移/审批伪造/注入）；
PII 默认脱敏（宁可多掩不漏）；审计只追加 + actor 认证；decided_by 不可伪造；
tenant-first + 资源级授权；X-Api-Key 本地身份；用户输入/RAG 文档/工具返回 = 不可信；
提示注入双向防护；fail-closed/零副作用；"错误码不被改写成成功"；最小权限；
Agent Lab 沙箱隔离。

**面试问题与解答要点**：

**Q13.1 请系统性讲一遍你的安全设计（越权→副作用→伪造→注入）。**
解答要点：
- 越权：租户隔离（(tenant,id) 主键 + tenant-first 门禁 + API 认证身份）+ 客户资源级
  授权（D3）。跨租户：memory 403/PG 404。
- 重复副作用：幂等三元组唯一 + 占位 + 原键对账（D2/D10/D11）；审批版本 CAS 防止并发
  矛盾决定（D4/D5）；订单行锁防额度超卖（D6）；线程租约防双推进（D9）。
- 非法迁移：规则状态机纯函数 + DB CHECK + 版本 CAS（D4-D6）。
- 伪造审批：approve/reject 需 APPROVER 角色 + decided_by=认证 principal + 决定带版本；
  resume 不带"批准"语义、伪造 resume 被忽略（g11）。
- 注入：查询注入拒绝检索（POLICY_INJECTION_DETECTED → 转人工）；文档投毒块排除；
  模型内容守卫兜底金额/审批/状态/执行指令；多 Agent 本地轻量守卫同语义。
- 每条对应回归：D1-D12 台账 + security/blocker invariants——面试可指到具体测试文件，
  说服力远大于背口号。

**Q13.2 脱敏策略为什么是"宁可多掩、不可漏"？在哪些位置执行脱敏？**
解答要点：
- redact.py 保守原则：完整手机/邮箱/身份证一律掩码；不确定的格式宁可按疑似掩码。
- 位置：① 日志（logging_config 任何路径不打印完整 PII）；② 模型上下文发送前
  （MODEL_EVALUATION §2：发送内容无完整手机号，测试断言）；③ Agent Lab / 四角色 trace
  摘要写入前掩码。
- 业务数据层：订单 customer_id 等结构化字段在受控数据层保留（业务需要），日志与模型
  上下文只用脱敏文本——"展示/日志脱敏，事实层受控保留"。
- 为什么不把所有字段删掉：业务与审计需要关联 customer_id；删了无法服务；
  正确做法是分层权限 + 展示脱敏。
- 面试升级：能区分"静态脱敏（写入前改掉）"与"动态脱敏（存储原值、查询时按权限
  脱敏）"取舍——本项目是前者为主（合成数据演示）+ 事实层受控。

**Q13.3 提示注入为什么在这里特别危险？你做了哪几层防护？**
解答要点：
- 危险：注入可能让 Agent "以为用户是管理员/忽略政策/直接放行"——一旦成功就是越权或
  伪造审批（金融/售后场景直接资损）。
- 层 1（检索侧）：查询注入 → InjectionDetected → 拒绝检索、不返回文档、转人工；
  文档投毒 → 块被排除不进 evidence。
- 层 2（模型侧）：内容守卫 assert_output_safe 拦截输出中的金额/审批/状态/执行指令；
  四角色 Resolution 无金额字段 + 文本守卫。
- 层 3（业务侧）：即使上面全被绕过，写路径仍需角色门禁 + 领域重算 + 审批版本——模型
  永远无法"说一句就退款"。
- 宪法第五条原话支持：用户输入、RAG 文档、工具返回都是不可信数据；其中的提示/角色
  声明/工具指令不能改变宪法。
- 面试表达：防护要"分层 + 纵深"，任何单层被击穿都不等于系统被攻破。

**Q13.4 审计与合规：谁能看审计？审计能防什么？不能防什么？**
解答要点：
- 谁能看：GET /audit 需 agent/approver/system 角色，且按租户作用域返回（内存按实体
  归属/PG 按行租户），不能跨租户看审计。
- 能防/能查：事后追溯谁在何时对哪张工单/操作做了什么（actor/before/after/action）；
  审批 decided_by 落到具体 principal——"哪个审批人批的"不可抵赖。
- 不能防：不能防"授权人故意滥用"（合规需要额外审批流/双人复核，不在 V1）；审计是
  检测/追溯手段不是权限控制手段。
- 面试诚实点：能主动说"审计保证可追溯，不保证决策正确——决策正确由领域规则 + 版本
  CAS + 人工审批保证"，避免把审计吹成万能。

**Q13.5 如果未来接真实客户数据（而非合成数据），你的架构还差什么？**
解答要点（开放题，展现合规意识）：
- 认证：X-Api-Key 本地身份 → 真实身份提供方（OIDC/企业 SSO），密钥管理服务而非
  本地 registry。
- 数据：真实客户 PII 的加密存储/传输、字段级权限、数据保留与删除策略（GDPR 类）、
  审计留存策略。
- 网络：出网白名单、真实支付/CRM 的签名与回调校验（现在 execute/reconcile 是模拟）。
- 治理：双人复核、限额分级、灰度与回滚、安全评审。
- 表述框架：先说"当前是合成数据沙箱 + 本地身份，安全链的**结构**（授权/审计/脱敏/幂等）
  已就位"，再列"接真实数据必须补的**设施层**"——证明你分得清"设计"与"部署成熟度"。

---
# 阶段 14：性能、扩展性与"不做"清单

**学习目标**：能讲清当前系统在性能/扩展上的真实选择（索引、连接池、事务边界、缓存、
向量库协议预留），以及"为什么很多热门东西没做"——用取舍逻辑而非"没时间"回答。

**重点阅读**：
- `src/repo/schema.sql`（索引：审计 (tenant,entity_type,entity_id) 等；主键即租户索引）
- `src/repo/postgres.py`（连接/事务使用方式；可讨论连接池未来项）
- `src/rag/vector.py`（VectorBackend 协议注释：pgvector/Milvus 规划）
- `src/platform/tooling.py`（工具超时 TOOL_TIMEOUT；写工具禁止超时盲目重试）
- `src/models/openai_compatible.py`（超时/重试/429 语义——LLM 侧性能与熔断）
- `docs/V1_EXECUTION_PLAN.md`、`docs/ARCHITECTURE.md`（扩展边界）
- 关注：哪些地方刻意**没有**做缓存/队列/向量库/长期记忆

**核心概念与术语**：复合主键即租户过滤索引；审计索引 (tenant,entity_type,entity_id)；
唯一约束（幂等/审批）；FOR UPDATE 行锁串行化额度；单事务边界 = 一致性而非吞吐；
tool_timeout（只读可放弃、写必须对账）；LLM 超时/重试/429/5xx 错误分类与降级；
VectorBackend 可插拔协议；pgvector/Milvus/长期记忆/缓存均"规划或不做"；Golden 集即
回归性能护栏（正确性优先于性能）。

**面试问题与解答要点**：

**Q14.1 你做了哪些数据库层面的性能与一致性设计？哪些索引是刻意的？**
解答要点：
- 主键都用 (tenant_id, 业务id)：天然按租户聚簇，tenant-first 查询走主键；审计
  (tenant,entity_type,entity_id) 建索引支撑按实体查审计。
- 唯一约束当并发工具用：幂等三元组唯一（并发重复插入只有一单成功）、approval
  (tenant,operation,decided_version) 唯一（并发审批只有一个决定）——"用约束换正确性"。
- FOR UPDATE 行锁：execute/reconcile 前锁 orders 行，串行化额度聚合——牺牲小并发换
  资损安全。
- 诚实点：数据量小（合成），没有做查询优化的大头工作；但"约束/锁/事务边界"这些
  结构选择在真实规模下依然成立，索引/分区是后续随量级演进。
- 面试反诘准备：被问"为什么不用 Redis 缓存订单？"——答：订单是强一致事实（退款容量
  判断必须实时、幂等必须唯一），缓存引入过期不一致窗口；读路径已足够快，先正确后优化。

**Q14.2 PG 连接是怎么管理的？如果上生产你会怎么做连接池与超时？**
解答要点：
- 当前：本地开发容器 + SQLAlchemy 引擎（create_engine 每处使用、AUTOCOMMIT 隔离用于
  建库等）；live 测试脚本单次进程生命周期管理连接。
- 上生产会做：连接池（SQLAlchemy QueuePool/或 PgBouncer），pool_size/max_overflow/
  pool_pre_ping；请求级事务边界明确（命令服务单事务内短连接持有）；语句超时与锁等待
  超时（lock_timeout）防长时间持锁；只读副本按需（读放大时）。
- 面试表达：能说出"连接池解决建立开销，pool_pre_ping 解决陈旧连接，事务边界决定
  持有时长，锁超时防死锁拖垮"四个点，说明不是只会写 query。

**Q14.3 LLM 调用的性能与可用性怎么处理？超时/重试语义是什么？**
解答要点：
- 错误分类决定行为：ModelTimeoutError/NetworkError → 有限重试后失败；429 →
  ModelRateLimitedError；5xx → ModelHttpError；JSON 非法 → ModelParseError；schema 不匹配
  → ModelSchemaError；Token 预算超限 → ModelQuotaExceededError（未调用即拒）。
- 降级：主模型任何 ModelError → ModelGateway 降级 OfflineRuleClient（标记 degraded）；
  工具层只读超时 → TOOL_TIMEOUT（可放弃）；写工具**禁止超时盲目重试** → 必须幂等对账
  （防重复副作用）。
- 记账：ModelInvocationMetadata 记 duration_ms/tokens/cost_estimate/degraded/error，
  为成本与质量监控留数据。
- 面试细节：能区分"读可降级/写不可盲重试"——这是可靠性的分水岭，很多方案在这栽跟头。

**Q14.4 为什么没有做：长期记忆、向量数据库、消息队列、缓存？哪些未来会做？**
解答要点（"不做"清单要用取舍讲）：
- 长期记忆：业务事实在 PG，流程状态在 checkpoint——"记忆"若指跨工单学习，需要真实
  数据与评测收益，当前无证据（宪法第十条）；不做。
- 向量数据库：数据量小 + VectorBackend 协议已预留（规划接入点）；有收益（更大政策库/
  语义召回）再替换，接口不变。
- 消息队列：审批/澄清是"等人"不是"任务洪峰"；执行外部副作用当前是模拟的同步调用；
  未来接真实支付可把"执行/对账"抽成队列任务（幂等键 + 原键对账不变）。
- 缓存：强一致写路径不缓存；只读政策/政策元数据未来可按版本缓存（版本失效即可）。
- 面试总结句："我的扩展点都是**协议化**的（VectorBackend/AfterSalesApplicationPort/
  RuntimeMode/能力矩阵），未来换实现不换接口；但没有对照数据前不引入新名词。"

**Q14.5 一个工作流线程的吞吐瓶颈在哪？如果并发工单量放大 100 倍会先遇到什么？**
解答要点（性能推演题）：
- 瓶颈预判：① PG 行锁/事务串行（额度扣减同订单并发）→ 高并发退款到同一订单会排队；
  ② 审批/澄清是人工等待（吞吐天然受限，与机器无关）；③ LLM 若接入则受外部延迟/限流；
  ④ checkpoint SQLite 单文件写并发。
- 放大 100 倍会先遇到：同订单/同客户并发热点锁竞争；审计表增长（追加索引查询仍快，
  需归档策略）；SQLite checkpoint 并发写（可换 PG checkpoint 表或分片）。
- 改进顺序：先做无锁读（只读副本/缓存政策），再分库分表按 tenant 分片（键天然带
  tenant），审计归档 + 只追加批量写。
- 面试加分：主动给出"先量化再优化"——黄金集/评测报告先保证正确性不变量不因优化
  回归（D12 报告确定性），再做压测定位热点。

---
# 阶段 15：难点复盘、失败案例与改进方向

**学习目标**：把项目里真实的"坑"变成面试素材：发生过什么、你怎么发现、怎么修的、
用什么防止复发。真实失败案例比完美叙事可信得多。

**重点阅读**：
- `docs/DEFECTS_LOG.md`（D1-D12 台账——每个都是真实修过的回归语义）
- `docs/STATUS_AND_RISKS.md`（当前风险矩阵）
- `docs/INTERVIEW_OVERVIEW.md` §6（建议顺序）
- git log（看提交消息里的修复脉络：存储约束/删除历史垃圾/PG guard/RAG 入默认闭环/
  demo 修复/报告墙钟修复…）
- `docs/TESTING_BASELINE.md`（基线数字演变背景）

**核心概念与术语**：回归台账 D1-D12；fail-closed 思维；no-fallback/fail-fast；诚实边界
（未实测不冒充）；ADR-002 回退条款；报告确定性（D12）；"删除优于保留"（删除历史退款
服务/Mule Bridge/SQLite 快照恢复原型）；用回归测试锁住自己犯过的错。

**面试问题与解答要点**：

**Q15.1 讲一个你踩过的最深的坑，以及你如何修复并防止复发。**
解答要点（选真实的，讲清"现象→根因→修复→防复发"四段）：
- 示例 A（D12 墙钟）：对照报告曾写入含 "ms" 的耗时行 → 报告含墙钟时间、两次运行
  diff 不一致 → 决定"报告不含逐 run 耗时、耗时仅 stdout" → 新增
  test_d12_reports_stable_no_wallclock_jitter 锁住"报告无 ms"。教训：可复现产物
  必须确定性；评测结论只用业务指标。
- 示例 B（D1 跨租户同单号）：内存 dict 以 order_id 为键，跨租户同 id 会静默覆盖 →
  seed_order 检测并拒绝 + PG 用 (tenant,order_id) 主键。教训：内存实现也要守住
  多租户语义，PG 语义才对。
- 示例 C（ps1 中文注释崩溃）：PowerShell 5.1 按 ANSI 读 UTF-8 脚本，中文注释解析失败
  → 脚本一律 ASCII-only + UTF-8 BOM。教训：工具链兼容性要实测，不假设。
- 表达结构：先给一句结论（"我学会了 X"），再 60 秒讲清细节，别流水账。

**Q15.2 你删掉过什么大块代码？为什么删？删除的决策依据是什么？**
解答要点：
- 删过：早期退款服务、Mule Bridge（跨服务协议假设）、SQLite 快照恢复原型、
  PgBackedSession 整库清空重插/镜像原型。
- 为什么删：双实现 = 双事实源风险 + 额外协议面 + 维护成本；Mule/整库镜像假设了
  "外部服务/整库复制"这种 V1 不需要的复杂度；删除只发生在确认无默认运行时依赖后
  （逐文件核对 import/测试引用）。
- 决策依据：AGENTS.md 第十条（不为了名词引入复杂度）+"删除优于保留孤立实验代码"
  （V1_EXECUTION_PLAN 第 8 条）；保留 row_codecs 等从原型拆出的**仍被使用**的部分。
- 面试价值：能讲"我不仅会写代码，还会判断什么代码不该存在"——架构师思维。

**Q15.3 项目里哪些结论是你"后来发现自己差点说错"的？怎么保证文档可信？**
解答要点：
- 例子：测试基线数字多次随实现漂移（450→439→428→394/438→399/443 等历史版本）——
  若文档不随代码同步就会把旧数字当结论；文档每次变更都要求"与实测输出一致"并标注
  日期与运行模式；README/TESTING_BASELINE/STATUS 三角同步。
- 例子：曾出现"把 RAG 检索到旧版政策当引用命中"的统计口径问题 → 改为三分类口径
  （现行命中/安全拒绝/注入单列），并写 citation metrics 测试。
- 机制保证：宪法（AGENTS.md）强制"文档与代码/测试同步 + 不得把规划写已实现"；
  git 提交消息带真实实测数字；面试前必须按当前 HEAD 重跑，不用旧报告替代本次实测
  （INTERVIEW_OVERVIEW §3 注）。
- 面试表达：主动讲"我维护了一套防自己说谎的机制"，比任何口头保证都有力。

**Q15.4 如果重做这个项目，你会保留什么、改变什么？**
解答要点：
- 保留：① 确定性领域服务与 Agent 职责分离（架构核心）；② 安全不变量 + 回归台账
  （测试即文档）；③ 诚实边界标注（面试可信度来源）；④ Port/协议化可替换设计。
- 改变：① 一开始就把 HTTP Agent 主链路纳入（避免"双入口"叙述短板）；② 更早引入
  隔离 PG live 测试纪律（避免早期用内存结论凑数）；③ 评测管线与文档基线同步机制
  自动化（数字漂移靠脚本校验而非人肉）；④ 若真实 LLM 是目标，从第一天就接影子评测
  收集失败样本，而不是后期补。
- 面试收尾句：把"如果重做"答成"我会把这次踩坑的教训前置"，展现迭代学习能力。

**Q15.5 你认为这个项目最大的技术风险是什么？现在有什么缓解？**
解答要点：
- 风险排序（对齐 STATUS_AND_RISKS）：① 真实 LLM 行为未知——缓解：离线基线 + 能力
  矩阵禁用高风险 + 内容守卫 + 领域终裁（即使真模型异常也无法越权）；② PG 依赖本地
  Docker/迁移——缓解：POSTGRES.md 一键复跑 + guard 隔离 + CI alembic；③ 双入口未合并
  ——缓解：文档如实标注 + 列为最高优先级增量；④ 合成数据不代表真实收益——缓解：
  评测/报告/面试口径全部标注合成边界。
- 诚实收尾：风险不是"说出来减分"，而是"回避被追问更扣分"；能排优先级 + 给缓解
  方案 = 资深工程师信号。

---
# 面试前自检清单（快速验证是否真正理解项目）

> 用法：对每一项能**不看代码直接答出**并**说出依据文件/命令**才算过。60 分以下别去面试。

## A. 一句话层（每题 10 秒）
- [ ] 能用 90 秒讲清：项目定位、主张（何时安全停止）、价值、证据（数字）、诚实边界。
- [ ] 能报出当前基线：内存 399 passed/44 skipped；隔离 PG 443 passed/0 skipped；
      隔离双跑 25×2；golden_v1 11/11、golden_v2 120/120。
- [ ] 能说清"PG 是唯一业务事实源、SQLite 只存流程状态"并解释为什么必须如此。
- [ ] 能说出"真实 LLM 未实测、未微调、非生产部署"且不慌。

## B. 架构层（每题 30 秒）
- [ ] 能徒手画出四层架构与两个入口，标注每层"负责/不负责"。
- [ ] 能解释 AfterSalesApplicationPort 为什么让 Memory 与 PG 可互换，且上层不分支。
- [ ] 能讲清"resume 只触发重读，审批语义来自数据库带版本决定"。
- [ ] 能解释 RuntimeMode 四值与 llm 未配 Key 的回落，且指出"模式只换决策来源、
      不换业务护栏"。

## C. 可靠性层（深挖题，每题能讲 1-2 分钟）
- [ ] 幂等：同键同载荷/异载荷语义、三元组唯一、pending 占位、原键对账、禁换键重试。
- [ ] 并发：乐观版本 CAS 用在哪、FOR UPDATE 行锁用在哪、为什么不能只用一个、
      单事务边界。
- [ ] operation_unknown 的完整收口流程（execute 超时 → unknown → reconcile 原键 →
      落账/关单）。
- [ ] workflow_threads 租约 + 请求指纹如何防"串请求"与"双推进"（D9）。
- [ ] 安全不变量六项能逐条说出"验证位置/回归编号"。

## D. RAG 与检索
- [ ] 检索管线（分词→倒排+向量→RRF→citation 校验）能画出。
- [ ] 能说清"检索只给证据、领域算金额"的边界与原因；空证据→转人工。
- [ ] 引用三分类口径（现行命中/安全拒绝/注入单列）能解释为什么这样统计。
- [ ] 查询注入 vs 文档投毒两条防护路径能讲清；VectorBackend 协议的意义。

## E. LLM 与评测
- [ ] 能力矩阵 6 项 + 4 低/2 高默认禁用；内容守卫拦截清单能背出。
- [ ] 降级链（primary→offline→转人工）与"零网络兜底"能讲清。
- [ ] 影子评测为什么零副作用；报告必填字段有哪些。
- [ ] 为什么对照报告不含耗时（D12）；为什么 A/B 无收益就维持单 Agent（ADR-002）。

## F. 工程与部署
- [ ] CI 实际跑什么、为什么 CI 的 PG live 是 skip 口径、run #1 结果如实表述。
- [ ] PG guard 拒绝哪些 URL、为什么目标只来自 OPSPILOT_TEST_DATABASE_URL。
- [ ] alembic 0001→0005 演进主线能概述。
- [ ] 能说出 3 个真实失败案例（墙钟、跨租户覆盖、ps1 编码…）的"根因→修复→防复发"。

## G. 诚实与边界（最容易被追问，必须自然）
- [ ] 被问"接真实客户数据还差什么"能分层回答（认证/加密/网络/治理）。
- [ ] 被问"为什么不做多 Agent/微调/向量库/长期记忆"能用"取舍 + 对照证据"回答。
- [ ] 能主动指出"双入口未合并/真实 LLM 未实测"这两个最大短板并给排序方案。
- [ ] 面试全程不使用 PPT 数字之外的任何虚构指标（每个数字都对应可复现命令）。

## H. 实战预演（面试前必跑一遍，确认环境可用）
- [ ] `.\scripts\run_pg_tests_isolated.ps1` → ISOLATED DOUBLE-RUN PASS。
- [ ] 模式 B 全量（OPSPILOT_TEST_DATABASE_URL=隔离库）→ 443 passed, 0 skipped。
- [ ] `scripts\demo_interview.py` → 七场景全过。
- [ ] `scripts\demo_rag_policy.py` → 检索展示全过。
- [ ] `scripts\demo_supervisor_trace.py --mode multi_agent` → 8/8 全过。
- [ ] `scripts\demo_modes.py` → 模式回落演示全过。
- [ ] `evals\compare_modes.py` / `compare_agents.py` / `replay.py` → 11/11（或既有报告一致）。
- [ ] `git status` 干净或改动可解释；`git log -1` 能报出当前 HEAD。

> 自检评分参考：A 全过 = 基础可讲；A+B+C = 可撑 30 分钟深挖；A~G 全过 = 可撑 60 分钟
> 加系统设计追问；H 跑通 = 现场演示不翻车。祝面试顺利。

---
*本文档由 OpsPilot 仓库（HEAD `2db03e3`）代码与文档整理生成；所有数字以实测命令输出为准，
面试前请按 H 节重跑确认环境与基线未漂移。*
