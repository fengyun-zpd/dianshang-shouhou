# OpsPilot 个人开发与业务设计手册

> 本手册围绕电商售后退款工单，整理个人开发中的业务分析、架构选择与可靠性设计。
> 文档更新：2026-09-16。采用合成样本；能力状态与实测结果分别以代码和[测试基线](./docs/TESTING_BASELINE.md)为依据。

## 内容导览

| 主题 | 设计关注点 |
| --- | --- |
| 1. 项目定位与业务价值 | 电商售后数字化、内部坐席与当前范围 |
| 2. 架构与信任边界 | Agent、领域服务、审批人与存储的职责 |
| 3. 数据模型 | PostgreSQL 事实、版本、租户与金额 |
| 4. 确定性业务规则 | 资格、状态迁移、错误与审计 |
| 5. 工作流与恢复 | 澄清、审批中断、重启与循环控制 |
| 6. 幂等与并发 | 同键语义、事务、租约及未知结果 |
| 7. 政策检索 | 证据来源、有效版本、注入防护 |
| 8. 模型建议层 | 能力矩阵、结构化输出与降级 |
| 9. 多角色编排实验 | 对照方法与业务更新尝试 |
| 10. API 与工作台 | 认证、资源授权、内部坐席流程 |
| 11. 评测与证据 | 黄金集、影子验证、质量评价 |
| 12. 测试与开发环境 | 隔离数据库、迁移、自动化入口 |
| 13. 安全与隐私 | 审计、脱敏、多租户与不可信输入 |
| 14. 性能与扩展 | 测量前提、扩展接口与业务边界 |
| 15. 缺陷复盘与维护 | 问题根因、验证证据和更新顺序 |

## 1. 项目定位与业务价值

OpsPilot 是个人开发的电商售后数字化工具，当前围绕内部坐席的退款工单实现完整处理路径。
请求整理、订单核验、政策查证、草稿审批、执行结果和审计记录形成一条可追踪的业务链。

希望改善的是信息反复查找、处理依据不清、重复请求和异常工单难以交接等问题。
这些是设计目标，尚未获得真实企业运营数据，不能据此给出节省工时或收益提升比例。
当前数据为固定种子合成样本，真实支付、企业身份、CRM 和客户自助入口未接通。

退款是已实现业务主线。退换货、补发、真实物流和客户自助需要额外业务设计，不能因为规则中提及
库存或高风险动作就推断相关业务已完成。拓展条件见[开发计划](./docs/DEVELOPMENT_PLAN.md)。

默认采用单 Agent：同一合成黄金集上，单 Agent 与可选多角色编排的结果均为 11/11，
尚未显示业务正确性收益。保留实验入口便于后续业务更新时重新比较，不将角色数量视为项目目标。

## 2. 总体架构与信任边界

```text
本地工作台 / HTTP 调用 / 回放脚本
                ↓
FastAPI 身份、参数、资源访问控制
                ↓
Agent 工作流：澄清 → 证据 → 草稿 → 等待 → 恢复 → 解释
                ↓
AfterSalesApplicationPort
    ├─ MemoryAdapter：本地合成场景与快速测试
    └─ PgCommandAdapter：事务内读取、校验、写入与审计
                ↓
PostgreSQL 业务事实 + SQLite 流程 checkpoint
```

HTTP 已接入 `start / clarify / decision / state`，脚本与 HTTP 可调用同一运行器。
接口层负责认证与参数边界，领域服务再次校验业务规则，避免业务裁决只存在于某个入口。
未配置运行器时返回 503；PG 配置或迁移不满足时启动失败，不以 memory 静默替代。

`AfterSalesApplicationPort` 将业务编排与存储实现分离。内存实现适合快速验证，PG 实现承担事务与持久化；
内存通过不能证明数据库并发正确。checkpoint 用于恢复流程位置，订单金额、审批决定和执行结果必须重新读取领域事实。

审批流程分为“提交决定”和“触发恢复”：授权人先将带版本的决定落库，再调用 `decision`。
该请求体不接收审批结论，模型、聊天文本和流程快照均不能代替审批。

详细接口与信任边界见[架构设计](./docs/ARCHITECTURE.md)。

## 3. 数据模型与 PostgreSQL 事实源

核心数据包括订单及明细、工单、退款操作、审批决定、政策、幂等记录、审计事件和工作流线程租约。
业务查询显式限定租户；PG 通过租户与实体标识定位事实，防止不同企业相同订单编号串用。

金额使用十进制定点语义，不能通过二进制浮点近似进行退款资格和剩余额度裁决。
规则与数据库约束共同限制金额、状态和版本；是否还能退款取决于最新订单与操作事实，不能使用模型记忆。

`PgCommandService` 在事务内重读事实、获取必要的锁、校验规则，写入业务行、审批、审计及幂等记录。
行编解码负责数据库记录与领域对象转换，不能在恢复时整库清空再回填流程快照。
早期整库镜像原型已清理，当前不得将其作为持久化方案。

迁移 `0006` 为审计增加持久 `event_id`：新事件有生成的标识，迁移中的历史行按数据库行 ID 回填。
无标识的旧领域对象仍存在兼容回退，不能声称所有来源都具备非空全局唯一约束。
流程视图基于租户和绑定实体重建审计引用。

数据准备与运行条件见[PostgreSQL 使用说明](./docs/POSTGRES.md)。

## 4. 确定性领域服务与业务规则

领域规则负责验证请求是否有权限、订单是否适用、政策是否有效、金额是否超出可退范围、
状态是否允许变更以及审批版本是否匹配。Agent 可以提出结构化草稿请求，不能改变这些判断。

关键错误按原义传播，例如 `TENANT_MISMATCH`、`PERMISSION_DENIED`、
`DECISION_VERSION_MISMATCH` 和 `AMOUNT_EXCEEDS_REMAINING`。
失败必须返回明确状态与处理要求，不能靠解释文本将拒绝改成成功。

退款需先形成草稿并提交审批。审批同意只说明满足审批条件，后续执行仍需校验当前状态与并发事实。
执行成功或审批拒绝后可以按领域规则收尾；执行失败保留工单并转人工，未知结果必须先对账。
关单失败保留原错误码，之后恢复只做允许的收尾，不重新执行退款。

业务测试优先断言数据库终态、金额、审批、审计与外部执行次数，避免只验证接口返回了 200。

## 5. 单 Agent 工作流、状态与恢复

工作流使用 LangGraph 组织请求解析、证据获取、规则草稿、审批中断、事实重读、执行和解释。
缺少信息时挂起澄清，补充信息后继续原线程；证据不足、越权或不可恢复错误时转人工。

审批节点的 `interrupt()` 保存流程恢复点；恢复后再次读取领域审批事实。
`approved=true` 等文本不能推进审批，HTTP 对额外结论字段返回 422。
等待与恢复是流程行为，业务事实以已提交的数据库记录为准。

线程身份是 `(tenant_id, thread_id)`。checkpoint 使用版本化长度编码，避免简单拼接的冒号碰撞；
旧键只有在内嵌租户、线程精确匹配时才可兼容读取，歧义旧键拒绝。

PG 模式使用固定 SQLite checkpoint 和数据库线程租约。旧进程中断后，新进程须等待租约到期，
再用相同数据库与 checkpoint 继续处理。已有独立进程顺序重启验证，多实例同时运行仍未验证。
内存模式重启后重置，不具备持久恢复承诺。

节点进入时检查步数，默认上限 32，另有 LangGraph recursion limit。超限后返回
`AGENT_LOOP_DETECTED` 并转人工，被拦截节点不再执行；终态持久化供新实例读取。
进入即 `interrupt()` 的节点写入可能被丢弃，因此持久化 `step_count` 不等于所有实际进入次数。

## 6. 幂等、并发与外部未知结果

| 情况 | 规定行为 | 业务原因 |
| --- | --- | --- |
| 同租户同键、同载荷 | 返回原结果 | 避免重复退款或重复建单 |
| 同租户同键、异载荷 | 拒绝冲突 | 防止原请求被替换 |
| 不同租户使用同一键 | 各自限定作用域 | 不串业务与结果 |
| 两人审批同一版本 | 版本比较与更新只允许合法决定 | 防止过期审批覆盖新事实 |
| 并发操作同一订单 | 事务内获取锁并核对剩余额度 | 防止合计退款越界 |
| 外部调用结果未知 | 按原操作查询与对账 | 超时不能证明对方没有执行 |

幂等键从首次调用持续传递。未知结果不能换键重试，否则可能在外部已经退款后再次执行。
对账成功后落账并收尾，对账失败保留失败状态和人工处理要求，仍未知时继续等待。

线程租约解决同一流程被多个运行器推进的问题，订单锁和版本校验解决业务资源竞争；
二者不能互相替代。只读工具去重可减少同线程重复查询，但 `get_operation` 的审批事实重读不缓存。

验证依据包括[缺陷台账](./docs/DEFECTS_LOG.md)中的 D2、D4–D6、D9–D11 及 PG 集成用例。

## 7. 本地政策检索与证据管理

本地检索用于找出与售后原因相关的政策证据；PolicyStore 配合检索、评分与引用校验提供结果。
现有检索模块包含词项匹配、向量后端抽象和融合排序等实验能力，不能据此声称已部署企业向量数据库。

证据须来自真实存在的文档分块，满足租户、启用、版本、生效和适用范围要求，并保留定位。
召回文本只用于解释和规则输入，退款金额、资格与状态由领域服务重新计算。
检索为空、政策不适用或引用无法验证时澄清或转人工，不能补造政策。

用户查询、检索文档及工具返回均是不可信数据。提示注入检测与引用校验分别处理恶意指令和无效来源；
文档中的“忽略规则”“直接批准”不能扩大 Agent 权限。模型发送前还需对通过检测的证据块脱敏。

指标应区分现行政策命中、无效旧版的安全拒绝以及注入场景，避免将安全拒绝简单计为检索失败。
引用命中率不能替代退款结果正确性，具体结果以 `evals/reports/` 的对应报告为准。

## 8. LLM 建议层与安全降级

模型只承担低风险语言任务，例如意图抽取、缺参识别、澄清问题和解释文本。
能力矩阵、结构化 Schema 和内容守卫限制输出；金额、审批、状态迁移、幂等和工具权限仍由确定性代码管理。

模型运行时包含配置、客户端协议、结构化输出、版本化 Prompt、离线规则、兼容 HTTP 适配器与路由层。
配置缺失、地址不在白名单或内容不满足约束时，降级离线或给出转人工信号。
超时、限流、非法 JSON、Schema 不匹配和预算超限需要分别记录，不能硬编码成功响应。

真实候选调用要求安全 Key、允许的 Base URL 和显式模型名。价格不是联网必需条件，
缺少输入或输出单价时相应成本显示 `N/A`。凭证仅通过安全配置提供，不能写入源码、报告或日志。

证据先做注入检测，通过后脱敏再参与预算与请求拼接；输入、输出 token 与成本分别记录。
离线 `tokens=0` 仅说明没有模型调用，不能当作真实模型成本指标。
真实模型与真实 Judge 尚未实测；适配器的替身测试不等于真实服务验证。

微调须先建立 Prompt、RAG 与结构化输出基线。LoRA/QLoRA/SFT 不承载动态订单或政策事实，
DPO 需要质量可控的偏好数据。当前仅有实验入口及合成样本，未训练，无效果结论。
详情见[模型评测](./docs/MODEL_EVALUATION.md)。

## 9. 运行模式与多角色编排实验

运行模式包含默认 `single_agent`、可选 `multi_agent`、`offline_rule` 与受控 `llm`。
模式调整语言或证据组织来源，不改变退款审批、金额计算和写入边界。

Supervisor 实验包括订单、历史、政策三路只读证据，以及 Triage、Evidence、Resolution、RiskReview
四角色编排。子角色只能使用白名单只读工具；租户上下文、Schema、超时与审计保持一致。
最终草稿仍通过顶层领域入口，风险审查本身不能执行退款。

轨迹记录 trace_id、输入输出摘要、耗时、工具调用、引用和拒绝原因。
相同合成黄金集上的三种路径均为 11/11，业务 outcome 与退款金额一致，尚未测得额外业务收益。
对照报告不凭单次墙钟时间认定性能优势；真实多模型协作效果未实测。

后续业务更新如确实需要分角色，须先提出可量化目标并重跑对照，再决定是否调整默认路径。
实验记录见[多角色编排实验](./docs/MULTI_AGENT_EXPERIMENT.md)。

## 10. API、认证授权与本地工作台

Agent HTTP 接口与领域 API 位于同一 FastAPI 应用。认证确定调用身份，角色授权确定操作能力，
资源授权再次核验租户与订单归属。不能从业务请求体接受调用者自称的租户或角色。

`start / clarify` 仅面向 AGENT，`decision` 面向 APPROVER / SYSTEM，`state` 供三类内部身份读取。
同租户客户调用这些生命周期接口也被拒绝；客户自助入口需要另行设计。
错误租户与不存在线程统一 404，避免泄露其他租户资源是否存在。

请求模型禁止额外字段，澄清不能夹带金额、审批、身份或外部结果。审批使用带版本的领域接口；
`decision` 只负责恢复，返回的状态来自领域事实与受限公共视图。

工作台可查看案件、草稿、证据、流程轨迹与评测信息。`memory` 用合成数据，重启会重置；
`pg` 不注册合成数据重置路由，不自动灌入业务样本。Agent Lab 为隔离验证入口。
当前使用进程内本地身份注册表，企业单点登录、生产访问控制和部署尚未完成。

## 11. 黄金集、回放、影子评测与质量评价

黄金集优先核验最终业务状态、退款金额、权限、引用、重复副作用与状态迁移。
仓库报告包括 11 条核心集与 120 条扩展集，都是合成场景，不代表真实订单分布。
不同报告保留各自运行日期、数据集与模式，不能拼接成一次新的验证结果。

影子评测只对输入作预测，不创建业务服务或数据库连接，不写审计，不触发审批和退款。
记录模型、Prompt、数据集、意图与澄清结果、结构错误、降级、token、成本和延迟。
缺少安全配置时不联网，并将真实候选结果标为未实测。

Judge 只评话术相关性、完整性、清晰度与语气等质量，不替代金额、权限、状态或审批断言。
业务失败单独标记，不能被高分覆盖。真实 Judge 的分数未经运行及人工校准不能作为业务完成证明。

已验证报告写入 `evals/reports/`；未实测候选报告写入 `.runtime/reports/`。
单测使用临时目录，防止污染历史评测；报告中的合成样本边界必须保留。

## 12. 测试策略、PG 隔离与开发环境

测试包含单元、集成、HTTP 端到端、性质、安全和缺陷回归等层次。
当前记录的 2026-09-13 全量结果为离线 **502 passed / 55 skipped / 1 warning**、
隔离 PG **557 passed / 0 skipped / 1 warning**；这些不是 2026-09-16 的新执行结果。
离线跳过包含未启用的 PG 集成与迁移条件不满足的运行时检查，必须与 PG 实测区分。

2026-09-16 用例收集核对到 HTTP 生命周期 31 项、PG 接入 7 项、重启恢复 5 项、审计关联 5 项。
收集不执行测试，只用于核对文档数量。完整历史结果见[测试基线](./docs/TESTING_BASELINE.md)。

PG 破坏性集成和回放会重建 schema。测试目标必须显式来自 `OPSPILOT_TEST_DATABASE_URL`，
仅允许本机 `opspilot_test_*`，不回退共享 `DATABASE_URL`。非法地址在连接前拒绝，不能为方便而关闭守卫。
隔离双跑脚本创建两套带随机后缀的库，建库失败立即停止。

迁移必须明确指向目标库，当前 schema 要求为 `0006`。PG 命令路径、租约和并发结论需真实 PG 回归支持，
不能将 memory 结果或测试跳过当作数据库正确性证据。

CI 文件当前配置 PostgreSQL 服务、专用测试库、迁移及分层与全量回归；专用库在每个任务的独立服务中创建。
远程最近运行状态未在此次文档整理中核验，不能由配置文件推断运行已经成功。

本机先执行 `scripts/init_d_env.ps1`，使用 D 盘 `.venv` 与项目内临时目录、缓存。
PowerShell 脚本注意编码兼容性，不改变系统目录变量的含义，不向 C 盘写项目运行时文件。

## 13. 审计、脱敏、提示注入与多租户

审计用于回答谁在什么对象上执行了什么操作，不能证明业务判断本身正确。
流程按租户、线程绑定工单与操作过滤审计事实，重复查询去重，避免全局游标造成漏记或串入其他线程。
持久 `event_id` 提供稳定关联；旧数据兼容行为与迁移约束须保持明确。

公共 HTTP 视图只返回所需字段，不整体序列化 checkpoint；错误响应与校验回显同样脱敏。
原始请求指纹用于判断同线程是否同请求，脱敏后文字相同也不能合并不同原始载荷。
完整手机号、邮箱、身份证号等敏感字段不得进入公开响应、模型请求或日志。

输入、RAG 文档和工具结果都是不可信数据。身份以认证为准，审批以数据库为准，
金额以规则为准，政策以有效引用为准，外部结果以执行与对账事实为准。
这些约束需要分层落实，不能仅依赖模型遵守提示。

生产数据治理仍需企业身份、凭证管理、访问审计、备份、网络隔离与数据保留策略，当前未形成生产验证结论。

## 14. 性能、扩展性与企业业务拓展

当前没有生产压测、并发容量或真实模型吞吐结果。测试时长只是本机单次观测，不能转换为线上服务承诺。
计划优化前先定义输入规模、并发、失败率、延迟分位数和资源使用，再定位瓶颈。

现有扩展点包括应用 Port、向量后端协议、运行模式和模型客户端。替换实现仍须满足权限、
事实源、错误、幂等和审计契约，接口可替换不等于新实现已经通过验证。

缓存适合在明确一致性要求后考虑；订单剩余额度、审批状态等事实不能被过期缓存裁决。
只读调用可做有限重试，写操作结果未知时仍只能原键对账。连接池、索引或检索优化均需前后对照。

企业业务拓展优先评估退换货、补发、物流查询与客户自助，分别补充逆向物流、库存、客户身份等领域设计。
多实例部署需明确共享 checkpoint、租约、故障恢复和并行一致性；简单拆分本地文件不等于完成高可用设计。
新副作用、事实源变更与真实外部接入须按工程规则获得明确授权。

## 15. 缺陷复盘与持续维护

| 历史问题 | 根因与处理 | 维护要求 |
| --- | --- | --- |
| 同订单号跨租户覆盖 | 内存单键不足，改为拒绝冲突；PG 按租户作用域查询 | 保留跨租户回归 |
| 审批与金额并发冲突 | 仅先读后写不能保护临界区，使用事务、锁与版本校验 | 以 PG 终态与合计金额验收 |
| checkpoint 键碰撞 | 分隔符拼接有歧义，使用长度编码与旧键精确身份校验 | 同时验证写入和读取路径 |
| HTTP 敏感字段暴露 | 返回内部状态过多，采用公共白名单和统一脱敏 | 正常、错误、校验回显及日志都检查 |
| 审计遗漏或串线程 | 全局水位不能表达线程归属，改按领域实体过滤 | 重复读取与新实例结果一致 |
| 未知或已执行操作恢复不收尾 | 流程状态与领域事实衔接不足，恢复时重读并合法关单 | 不重复执行，失败保留原码 |
| 重启租约测试波动 | 进程与时间条件不真实，改用独立进程及数据库时间 | 区分租约未过期与可接续状态 |
| 对照报告受墙钟影响 | 单次耗时进入确定性对照产物 | 正确性结论与性能测量分开 |
| 文档与代码漂移 | 旧数字和未接入描述重复沿用 | 标明日期、提交、模式及来源，保持历史记录 |

整改细节见[可靠性审查记录](./docs/RELIABILITY_REVIEW_2026-09-13.md)。
历史失败不能删除，也不能将后来成功的数字覆盖到原失败轮次。

维护顺序遵循业务闭环、确定性边界、安全恢复、评测观测、界面体验、模型与性能优化。
保留正常路径和失败路径，记录未验证部分；仅有文档改动时核对内容、链接和依据，不虚构重新运行的业务结果。

## 开发核对清单

- [ ] 项目描述聚焦电商售后，当前能力与企业业务拓展计划分开。
- [ ] 业务修改明确状态、权限、错误、幂等、审批和审计语义。
- [ ] 工单、金额、审批与执行结果来自领域事实；checkpoint 仅用于流程恢复。
- [ ] 正常、拒绝、重复请求、未知结果、跨租户和恢复路径有对应验证。
- [ ] 测试环境与数据集明确，PG 跳过项不算通过，报告注明运行日期。
- [ ] 真实模型、微调、吞吐、企业接入等没有证据时保持未实测或未实现。
- [ ] 文档与代码一致，引用可定位，开发计划不包含已完成事项的过时指令。
- [ ] 已有用户文件和无关改动保留；提交与上传遵循当前授权。
